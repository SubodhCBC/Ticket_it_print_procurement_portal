/*
 * GENERATED by scripts/generate-openapi-manifest.mjs — do not edit by hand.
 *
 * Regenerate after adding, removing or changing a route:
 *   node scripts/generate-openapi-manifest.mjs
 */
import { Permission } from '@/server/auth/permissions'
import * as accountValidation from '@/server/accounts/account.validation'
import * as approvalValidation from '@/server/approvals/approval.validation'
import * as auditValidation from '@/server/audit/audit.validation'
import * as authValidation from '@/server/auth/auth.validation'
import * as invoiceValidation from '@/server/billing/invoice.validation'
import * as cartValidation from '@/server/cart/cart.validation'
import * as categoryValidation from '@/server/catalog/category.validation'
import * as productValidation from '@/server/catalog/product.validation'
import * as damValidation from '@/server/dam/dam.validation'
import * as orderValidation from '@/server/orders/order.validation'
import * as rateCardValidation from '@/server/pricing/rate-card.validation'
import * as reportValidation from '@/server/reports/report.validation'
import * as settingsValidation from '@/server/settings/settings.validation'
import * as shippingValidation from '@/server/shipping/shipping.validation'
import * as siteValidation from '@/server/sites/site.validation'
import * as templateValidation from '@/server/templates/template.validation'
import * as invitationValidation from '@/server/users/invitation.validation'
import * as userValidation from '@/server/users/user.validation'
import type { ManifestOperation } from './manifest.types'

export const ROUTE_MANIFEST: readonly ManifestOperation[] = [
  {
    method: 'get',
    path: '/api/v1/accounts',
    tag: 'accounts',
    summary:
      'Cross-tenant by definition, so it runs outside any tenant scope and RLS does not bound it.',
    description:
      'Cross-tenant by definition, so it runs outside any tenant scope and RLS does not bound it. ACCOUNT_MANAGE is the whole protection — it is ADMIN-only in the role baseline, and no customer role holds it.',
    auth: 'bearer',
    permissions: [Permission.ACCOUNT_MANAGE],
    roles: [],
    body: [],
    query: [accountValidation.ListAccountsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/accounts/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/accounts',
    tag: 'accounts',
    summary:
      'Refuses a code whose slug collides with an existing account: the slug is how a legacy login finds…',
    description:
      "Refuses a code whose slug collides with an existing account: the slug is how a legacy login finds its account, so a collision would silently attach that customer's users to this new row.",
    auth: 'bearer',
    permissions: [Permission.ACCOUNT_MANAGE],
    roles: [],
    body: [accountValidation.CreateAccountSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/accounts/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/accounts/{accountId}',
    tag: 'accounts',
    auth: 'bearer',
    permissions: [Permission.ACCOUNT_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/accounts/[accountId]/route.ts',
  },
  {
    method: 'patch',
    path: '/api/v1/accounts/{accountId}',
    tag: 'accounts',
    summary:
      'A status change is additionally logged as its own audit action rather than folded into the diff…',
    description:
      'A status change is additionally logged as its own audit action rather than folded into the diff — "who suspended this customer and when" is a question people ask, and it should not require reading a field-level diff to answer.',
    auth: 'bearer',
    permissions: [Permission.ACCOUNT_MANAGE],
    roles: [],
    body: [accountValidation.UpdateAccountSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/accounts/[accountId]/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/accounts/{accountId}',
    tag: 'accounts',
    summary: 'Soft: invoices, orders and audit entries reference the account.',
    description:
      'Soft: invoices, orders and audit entries reference the account. The users are deliberately left alone rather than cascaded — a mistaken deactivation must not have rewritten hundreds of rows on the way.',
    auth: 'bearer',
    permissions: [Permission.ACCOUNT_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: false,
    source: 'src/app/api/v1/accounts/[accountId]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/approvals',
    tag: 'approvals',
    summary: 'The approvals queue.',
    description:
      'The approvals queue. `mine=true` narrows to steps this actor can act on right now — open, at the current tier, and addressed to them by name or by role — and never their own order, whatever role they hold.',
    auth: 'bearer',
    permissions: [Permission.APPROVAL_ACT],
    roles: [],
    body: [],
    query: [approvalValidation.ListApprovalsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/approvals/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/approvals/orders/{orderId}',
    tag: 'approvals',
    summary:
      'The approval an order is waiting on, so a buyer can see who it sits with without opening the…',
    description:
      'The approval an order is waiting on, so a buyer can see who it sits with without opening the approvals hub. An order from another account answers 404.',
    auth: 'bearer',
    permissions: [Permission.ORDER_VIEW_OWN],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/approvals/orders/[orderId]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/approvals/rules',
    tag: 'approvals',
    summary: 'The rules that decide which orders need approving and by whom.',
    description:
      'The rules that decide which orders need approving and by whom. USER_MANAGE rather than APPROVAL_ACT: deciding an order and deciding who decides orders are different jobs.',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/approvals/rules/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/approvals/rules',
    tag: 'approvals',
    summary:
      'A rule pointing at a category, branch or person that does not exist would match nothing or route to…',
    description:
      'A rule pointing at a category, branch or person that does not exist would match nothing or route to nobody and give no sign of either, so all three are checked before the row is written.',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [approvalValidation.CreateApprovalRuleSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/approvals/rules/route.ts',
  },
  {
    method: 'patch',
    path: '/api/v1/approvals/rules/{ruleId}',
    tag: 'approvals',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [approvalValidation.UpdateApprovalRuleSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/approvals/rules/[ruleId]/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/approvals/rules/{ruleId}',
    tag: 'approvals',
    summary: 'Soft.',
    description:
      'Soft. Requests already in flight keep their steps — the approver was snapshotted onto the step when it was raised — so retiring a rule never strands an order halfway through it.',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: false,
    source: 'src/app/api/v1/approvals/rules/[ruleId]/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/approvals/steps/{stepId}',
    tag: 'approvals',
    summary: 'Records one decision.',
    description:
      "Records one decision. The step, the request's new tier or outcome, the order's status and the entry on the order's timeline all commit together — an approved decision on an order still sitting in the queue would look to the approver like their click did nothing.\n\nAn order cannot be approved by the person who raised it, and a step addressed to someone else is refused.",
    auth: 'bearer',
    permissions: [Permission.APPROVAL_ACT],
    roles: [],
    body: [approvalValidation.DecideApprovalSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/approvals/steps/[stepId]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/audit-logs',
    tag: 'audit-logs',
    summary: 'Read-only by design.',
    description:
      'Read-only by design. There is no POST, PATCH or DELETE here and there should never be one — an audit trail an operator can write to or edit is not evidence of anything.\n\nNewest first, offset-paginated so the admin table can show a total and jump between pages.',
    auth: 'bearer',
    permissions: [Permission.AUDIT_VIEW],
    roles: [],
    body: [],
    query: [auditValidation.ListAuditLogQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/audit-logs/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/auth/login',
    tag: 'auth',
    summary: 'Authenticate with a portal or legacy credential.',
    description:
      'Authenticate with a portal or legacy credential. The first login for a user is verified against the legacy Ticket-IT database and replicates them into the portal database; later logins are served entirely from the portal database.',
    auth: 'public',
    permissions: [],
    roles: [],
    body: [authValidation.LoginSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: true,
    source: 'src/app/api/v1/auth/login/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/auth/logout',
    tag: 'auth',
    summary: 'Revoke a refresh token.',
    description:
      'Revoke a refresh token. Public and idempotent: a client holding an expired access token must still be able to end its session, and an unknown token is not an error.',
    auth: 'public',
    permissions: [],
    roles: [],
    body: [authValidation.LogoutSchema],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: false,
    source: 'src/app/api/v1/auth/logout/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/auth/me',
    tag: 'auth',
    summary:
      'The currently authenticated user, described exactly as login and refresh describe them — same…',
    description:
      'The currently authenticated user, described exactly as login and refresh describe them — same account, site and permission fields, so a client never loses information by rotating its token.',
    auth: 'bearer',
    permissions: [],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/auth/me/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/auth/refresh',
    tag: 'auth',
    summary: 'Exchange a refresh token for a new token pair.',
    description:
      'Exchange a refresh token for a new token pair. Presenting a token that has already been rotated revokes the whole session — see `rotateTokens`.',
    auth: 'public',
    permissions: [],
    roles: [],
    body: [authValidation.RefreshSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: true,
    source: 'src/app/api/v1/auth/refresh/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/authorization/permissions',
    tag: 'authorization',
    summary:
      "The permission catalogue and each role's baseline, so the roles-and- permissions screen renders the…",
    description:
      "The permission catalogue and each role's baseline, so the roles-and- permissions screen renders the matrix rather than hard-coding a second copy of it.\n\nRead-only, and it stays that way: the baseline is compiled into the application, so there is nothing here to PUT. Departures from it are per-user grants on `/users/:userId/permissions`.",
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/authorization/permissions/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/billing/invoices',
    tag: 'billing',
    summary:
      'A customer sees their own; an administrator may name an account or omit it for the cross-tenant…',
    description:
      'A customer sees their own; an administrator may name an account or omit it for the cross-tenant list. `overdue=true` narrows to issued invoices past their due date and not yet settled.',
    auth: 'bearer',
    permissions: [Permission.BILLING_VIEW],
    roles: [],
    body: [],
    query: [invoiceValidation.ListInvoicesQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/billing/invoices/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/billing/invoices/{invoiceId}',
    tag: 'billing',
    summary: 'One invoice with every line.',
    description:
      "One invoice with every line. Another account's invoice answers 404: confirming that an invoice number belongs to somebody discloses that they are a customer.",
    auth: 'bearer',
    permissions: [Permission.BILLING_VIEW],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/billing/invoices/[invoiceId]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/billing/invoices/{invoiceId}/csv',
    tag: 'billing',
    summary:
      'The transaction-level backing file (SOW O-6, §15): one row per billed item, every order line and…',
    description:
      "The transaction-level backing file (SOW O-6, §15): one row per billed item, every order line and each order's delivery charge, with the order, site, buyer, delivery and status columns B-01 to B-11 list. `Line value` sums to the subtotal and `Line total payable` to the total.\n\nCells that begin with a formula character are prefixed with an apostrophe: an export like this is opened on a machine with access to the ledger, which is the worst possible place for a spreadsheet injection.",
    auth: 'bearer',
    permissions: [Permission.BILLING_VIEW],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    rateLimited: false,
    source: 'src/app/api/v1/billing/invoices/[invoiceId]/csv/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/billing/invoices/{invoiceId}/issue',
    tag: 'billing',
    summary: 'Numbers a draft and freezes it.',
    description:
      'Numbers a draft and freezes it. After this nothing changes what the customer was billed — a mistake becomes a void and a reissue.\n\nThe number comes from a counter table rather than a sequence, because several jurisdictions require invoice numbers to be unbroken and a sequence does not roll back.',
    auth: 'bearer',
    permissions: [Permission.BILLING_MANAGE],
    roles: [],
    body: [invoiceValidation.IssueInvoiceSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/billing/invoices/[invoiceId]/issue/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/billing/invoices/{invoiceId}/paid',
    tag: 'billing',
    summary: 'Settles an issued invoice.',
    auth: 'bearer',
    permissions: [Permission.BILLING_MANAGE],
    roles: [],
    body: [invoiceValidation.MarkInvoicePaidSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/billing/invoices/[invoiceId]/paid/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/billing/invoices/{invoiceId}/pdf',
    tag: 'billing',
    summary:
      "Rendered on demand from the invoice's own frozen rows, and deliberately not stored: a saved PDF…",
    description:
      "Rendered on demand from the invoice's own frozen rows, and deliberately not stored: a saved PDF would be a second copy of the truth, and the two would eventually disagree.",
    auth: 'bearer',
    permissions: [Permission.BILLING_VIEW],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    produces: 'application/pdf',
    rateLimited: false,
    source: 'src/app/api/v1/billing/invoices/[invoiceId]/pdf/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/billing/invoices/{invoiceId}/void',
    tag: 'billing',
    summary: 'Cancels an issued invoice and keeps its number.',
    description:
      'Cancels an issued invoice and keeps its number. An invoice number that simply disappears is exactly what a tax audit asks about — "voided" is an answer, "missing" is not.\n\nVoiding frees the orders to be billed again, which is how a corrected invoice is produced: void, regenerate, reissue.',
    auth: 'bearer',
    permissions: [Permission.BILLING_MANAGE],
    roles: [],
    body: [invoiceValidation.VoidInvoiceSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/billing/invoices/[invoiceId]/void/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/billing/invoices/{invoiceId}/xlsx',
    tag: 'billing',
    summary:
      'Two sheets: every order, and the per-branch summary that goes in a board pack.',
    auth: 'bearer',
    permissions: [Permission.BILLING_VIEW],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    rateLimited: false,
    source: 'src/app/api/v1/billing/invoices/[invoiceId]/xlsx/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/billing/invoices/generate',
    tag: 'billing',
    summary: 'Builds or rebuilds the draft for one account and period.',
    description:
      'Builds or rebuilds the draft for one account and period. Rebuilding replaces the lines wholesale: an order cancelled since the last run has to disappear from the draft, and a merge that only added would keep billing it.\n\nAn order already frozen onto an issued invoice is skipped, however many times this runs.',
    auth: 'bearer',
    permissions: [Permission.BILLING_MANAGE],
    roles: [],
    body: [invoiceValidation.GenerateInvoiceSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/billing/invoices/generate/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/billing/periods/{billingPeriod}',
    tag: 'billing',
    summary: 'The KPI cards for one `YYYY-MM`.',
    description:
      'The KPI cards for one `YYYY-MM`. `unbilledTotal` is the number worth having: orders in the period that have shipped but sit on no issued invoice. Without it a month can look fully settled while a dozen orders quietly fall outside every invoice.',
    auth: 'bearer',
    permissions: [Permission.BILLING_VIEW],
    roles: [],
    body: [],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/billing/periods/[billingPeriod]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/cart',
    tag: 'cart',
    summary:
      "This user's open basket for a branch, created on first use — showing the cart is every client's…",
    description:
      "This user's open basket for a branch, created on first use — showing the cart is every client's first action, and making them POST an empty one first would be a round trip that exists only to satisfy REST.\n\nBaskets are never addressed by id: the basket is resolved from the authenticated user and the branch they asked for, so there is no identifier for one colleague to guess another's with.",
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [],
    query: [cartValidation.CartQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/cart/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/cart',
    tag: 'cart',
    summary: 'Empties the basket but keeps it.',
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [],
    query: [cartValidation.CartQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/cart/route.ts',
  },
  {
    method: 'patch',
    path: '/api/v1/cart/checkout-details',
    tag: 'cart',
    summary:
      'Purchase order, addresses, delivery date, payment method and terms.',
    description:
      "Purchase order, addresses, delivery date, payment method and terms.\n\nThe branch cannot be changed here: a basket belongs to one branch, and that branch decides its budget, its purchase-order rule and its addresses. Asking for the other branch's basket is the operation; moving this one is not.\n\n`acceptTerms` is a boolean, and the server stamps the instant — taking a timestamp from the client would let it claim the buyer agreed at any time it liked.\n\nChanging `shippingAddressId` resets the basket's NZ Post delivery choice to that address: to the address itself when it has been validated through NZ Post (its DPID and rural flag), and to nothing when it has not (SOW F-16).",
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [cartValidation.SetCheckoutDetailsSchema],
    query: [cartValidation.CartQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/cart/checkout-details/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/cart/checkout-session',
    tag: 'cart',
    summary:
      'The validated basket, refused unless it is ready to become an order.',
    description:
      "The validated basket, refused unless it is ready to become an order.\n\nDeliberately creates nothing: no order, no stock reservation, no change to the basket's status. All three belong to the order write, and doing any of them here would leave the system half-committed whenever that write failed.\n\nThe response carries `approval` — whether placing now would need approval, decided by the same rules placement uses — and `deliveryNotesRequired`.",
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [],
    query: [cartValidation.CartQuerySchema],
    extraQuery: [],
    successStatuses: [201],
    errors: [
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description:
          'The basket is not ready to be ordered. `details.issues` lists every problem, each with a stable `code` such as `SHIPPING_METHOD_REQUIRED` or `BUDGET_EXCEEDED`.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/cart/checkout-session/route.ts',
  },
  {
    method: 'put',
    path: '/api/v1/cart/delivery-address',
    tag: 'cart',
    summary:
      "Delivers the basket to an address typed at checkout instead of one of the branch's saved ones (SOW…",
    description:
      "Delivers the basket to an address typed at checkout instead of one of the branch's saved ones (SOW F-18: \"alternate ship-to entry enabled or disabled per account by configuration\"). The address is kept for this basket and its order, and never appears in the branch's address list.\n\nPUT because it replaces the basket's typed address: sending a corrected one retires the previous one, unless an order already points at it.\n\nThe account's `allowCustomDeliveryAddress` setting is checked as well as the permission, so an account that has not switched it on answers 403 whatever the buyer holds. Whether the buyer may use it at all is on the cart validation (`customDeliveryAddress.allowed`), because buyers cannot read account settings.\n\nSend `nzPostAddressId` from `GET /api/v1/shipping/addresses` to have the address validated through NZ Post ParcelAddress (SOW F-16): the address lines are then NZ Post's, and the basket's NZ Post delivery choice — the DPID and the rural flag a label is sent with — is set to the same address in the same call. Typed fields alone are still accepted and are stored as typed.",
    auth: 'bearer',
    permissions: [
      Permission.ORDER_CREATE,
      Permission.ORDER_CUSTOM_DELIVERY_ADDRESS,
    ],
    roles: [],
    body: [cartValidation.SetOneOffDeliveryAddressSchema],
    query: [cartValidation.CartQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      {
        status: 403,
        code: 'FORBIDDEN',
        description:
          'The account does not allow typed delivery addresses, or this buyer may not use them.',
      },
      {
        status: 404,
        code: 'NOT_FOUND',
        description: 'NZ Post does not recognise `nzPostAddressId`.',
      },
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description: 'The basket has no branch yet.',
      },
      {
        status: 503,
        code: 'DEPENDENCY_UNAVAILABLE',
        description:
          '`nzPostAddressId` was sent while NZ Post is switched off or unreachable.',
      },
    ],
    conditionalPermissions: [
      { permission: 'ORDER_CREATE', when: 'Always.' },
      {
        permission: 'ORDER_CUSTOM_DELIVERY_ADDRESS',
        when: 'Always; granted to buyers by default and deniable per user.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/cart/delivery-address/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/cart/lines',
    tag: 'cart',
    summary:
      'Merges into an existing line only when both are for the same product and variant **and** neither…',
    description:
      "Merges into an existing line only when both are for the same product and variant **and** neither carries customisation. Two personalised runs of the same business card are two different things to print, and adding their quantities together would silently destroy one.\n\nA line naming a template has its values rebuilt from that template's editable layers, against the version currently published — so a value aimed at a locked layer cannot survive, and a stale version is sent back rather than pinned to artwork nobody can see any more.",
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [cartValidation.AddCartLineSchema],
    query: [cartValidation.CartQuerySchema],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/cart/lines/route.ts',
  },
  {
    method: 'patch',
    path: '/api/v1/cart/lines/{lineId}',
    tag: 'cart',
    summary:
      'Editing the values of a line that already names a template goes through the same rebuild the add…',
    description:
      'Editing the values of a line that already names a template goes through the same rebuild the add did: the template is taken from the line when the request is silent about it, so stripping `templateId` from the body cannot turn a checked personalisation back into a free-form bag of values.',
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [cartValidation.UpdateCartLineSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/cart/lines/[lineId]/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/cart/lines/{lineId}',
    tag: 'cart',
    summary:
      "A line belonging to a colleague's basket answers 404, not 403 — the tenant scope bounds the…",
    description:
      "A line belonging to a colleague's basket answers 404, not 403 — the tenant scope bounds the account, and the ownership check inside the tenant is what this adds.",
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/cart/lines/[lineId]/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/cart/normalise',
    tag: 'cart',
    summary:
      'Rounds every line up to a quantity the product can actually be ordered in.',
    description:
      'Rounds every line up to a quantity the product can actually be ordered in.\n\nThe explicit half of "report the adjustment rather than applying it": validate reports the rounding as a warning, and nothing is changed until the buyer asks for it here. A customer who typed 120 of something sold in fifties gets 150, and gets to see that before it happens.',
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [],
    query: [cartValidation.CartQuerySchema],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/cart/normalise/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/cart/shipping',
    tag: 'cart',
    summary: "The basket's NZ Post delivery choice.",
    description:
      'The validated address, any collection point, and the chosen service with its quote. `selection` is null until an address has been chosen.\n\nThe quote is recorded with the order and never billed: `freightBilled` is always false, and nothing here changes the basket total or the branch budget.',
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [],
    query: [cartValidation.CartQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/cart/shipping/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/cart/shipping',
    tag: 'cart',
    summary: "Forget the basket's NZ Post delivery choice.",
    description:
      'The order can still be placed without one; staff then enter the delivery address when they request the label.',
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [],
    query: [cartValidation.CartQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/cart/shipping/route.ts',
  },
  {
    method: 'put',
    path: '/api/v1/cart/shipping/address',
    tag: 'cart',
    summary: 'Choose the NZ Post-validated delivery address.',
    description:
      'Takes an `addressId` from `GET /shipping/addresses`. The address is fetched from ParcelAddress again here, so its parts, DPID and rural flag come from NZ Post rather than from the client.\n\nClears any chosen service, quote and collection point: they were for the previous address.',
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [shippingValidation.SelectDeliveryAddressSchema],
    query: [cartValidation.CartQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/cart/shipping/address/route.ts',
  },
  {
    method: 'put',
    path: '/api/v1/cart/shipping/collection-point',
    tag: 'cart',
    summary: 'Collect from a nearby location instead, or stop.',
    description:
      'Takes a `collectionPointId` from `GET /cart/shipping/collection-points`, or null to go back to delivery to the address. The point is looked up again among the ones NZ Post offers for the chosen address.\n\nClears the chosen service and quote, which were for the previous choice.',
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [shippingValidation.SelectCollectionPointSchema],
    query: [cartValidation.CartQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      {
        status: 404,
        code: 'NOT_FOUND',
        description:
          'The collection point is not one NZ Post offers for the chosen address.',
      },
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description: 'No NZ Post address has been chosen for this basket yet.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/cart/shipping/collection-point/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/cart/shipping/collection-points',
    tag: 'cart',
    summary: 'Parcel collection locations near the chosen address.',
    description:
      "Backed by NZ Post's Collection Address API. Needs an address chosen with `PUT /cart/shipping/address` first; the locations are the ones NZ Post offers for that address, nearest first.",
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [],
    query: [shippingValidation.CollectionPointsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description: 'No NZ Post address has been chosen for this basket yet.',
      },
      {
        status: 503,
        code: 'DEPENDENCY_UNAVAILABLE',
        description:
          'NZ Post could not be reached, or collection points are not available to this portal yet.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/cart/shipping/collection-points/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/cart/shipping/options',
    tag: 'cart',
    summary: 'Delivery services and prices for the chosen address.',
    description:
      "Live ShippingOptions rates for the basket as one parcel, estimated from the products' recorded weights and a default box size. `source` says where the prices came from: NZPOST, FLAT_RATE when live rates could not be had, or UNAVAILABLE when there is neither — checkout carries on regardless.\n\nNeeds an address chosen with `PUT /cart/shipping/address` first.",
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [],
    query: [cartValidation.CartQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description: 'No NZ Post address has been chosen for this basket yet.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/cart/shipping/options/route.ts',
  },
  {
    method: 'put',
    path: '/api/v1/cart/shipping/service',
    tag: 'cart',
    summary: 'Choose a delivery service.',
    description:
      'Takes a `serviceCode` from `GET /cart/shipping/options`. The price is quoted again here and recorded as it stands, including mandatory addons such as the rural delivery surcharge.\n\nRecorded, never billed: the quote travels to the order for reconciliation and does not change the basket total, the branch budget or the invoice.',
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [shippingValidation.SelectShippingServiceSchema],
    query: [cartValidation.CartQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description:
          'No address chosen yet, or the service is not offered for this address (`details.available`).',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/cart/shipping/service/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/cart/validate',
    tag: 'cart',
    summary:
      'Everything wrong with the basket, at once — it never short-circuits, so a buyer with four bad lines…',
    description:
      'Everything wrong with the basket, at once — it never short-circuits, so a buyer with four bad lines sees four messages rather than fixing one and discovering the next.\n\nBlocking `issues` are separated from `warnings`: a quantity that needs rounding up is a warning the buyer can accept, while an unpublished product or a breached budget is a hard stop. `forCheckout` additionally requires the details a basket needs before it can be paid for.\n\nWrites nothing. POST because a validation prices the whole basket and that is not a GET-shaped amount of work, and 200 because nothing was created.',
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [],
    query: [cartValidation.ValidateCartQuerySchema],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/cart/validate/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/catalog/categories',
    tag: 'catalog',
    summary:
      'Unpaginated — there are eight of these, and the navigation renders them all.',
    description:
      'Unpaginated — there are eight of these, and the navigation renders them all. Readable by anyone who can see the catalogue: the category filter is the first thing the shop draws.\n\nThe answer depends on who asks: a RESTRICTED category is listed only to the accounts on its allow-list (SOW AD-5). `visibility` filters the list for administrators and is ignored for everyone else.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_VIEW],
    roles: [],
    body: [],
    query: [categoryValidation.ListCategoriesQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/categories/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/catalog/categories',
    tag: 'catalog',
    summary:
      "CATALOG_MANAGE, which only ADMIN holds: the taxonomy is the platform operator's, not a customer's.",
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [categoryValidation.CreateCategorySchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/categories/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/catalog/categories/{categoryId}',
    tag: 'catalog',
    summary:
      "A RESTRICTED category the caller's account is not on answers 404, the same as one that does not…",
    description:
      "A RESTRICTED category the caller's account is not on answers 404, the same as one that does not exist (SOW AD-5).",
    auth: 'bearer',
    permissions: [Permission.CATALOG_VIEW],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/categories/[categoryId]/route.ts',
  },
  {
    method: 'patch',
    path: '/api/v1/catalog/categories/{categoryId}',
    tag: 'catalog',
    summary:
      '`code` cannot be changed: it appears in import files and saved URLs.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [categoryValidation.UpdateCategorySchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/categories/[categoryId]/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/catalog/categories/{categoryId}',
    tag: 'catalog',
    summary:
      'Deactivates rather than deletes, and is refused while products still point at it — every catalogue…',
    description:
      'Deactivates rather than deletes, and is refused while products still point at it — every catalogue query joins through the category, so orphaning them would break the listing rather than tidy it.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/categories/[categoryId]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/catalog/categories/{categoryId}/visibility',
    tag: 'catalog',
    summary:
      'The accounts a restricted category is open to (SOW AD-5), which `PUT` replaces wholesale and…',
    description:
      'The accounts a restricted category is open to (SOW AD-5), which `PUT` replaces wholesale and nothing else returns — the panel that edits the list pre-fills from this.\n\nCATALOG_MANAGE rather than CATALOG_VIEW: it says which customers hold a contract category, which is commercial information about other tenants.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      { status: 404, code: 'NOT_FOUND', description: 'No such category.' },
    ],
    conditionalPermissions: [
      {
        permission: 'CATALOG_MANAGE',
        when: 'Always. Without it the answer is 403.',
      },
    ],
    rateLimited: false,
    source:
      'src/app/api/v1/catalog/categories/[categoryId]/visibility/route.ts',
  },
  {
    method: 'put',
    path: '/api/v1/catalog/categories/{categoryId}/visibility',
    tag: 'catalog',
    summary:
      'RESTRICTED hides the category, and every product in it, from any account not in `accountIds` — on…',
    description:
      "RESTRICTED hides the category, and every product in it, from any account not in `accountIds` — on top of each product's own visibility, which still applies. ALL_ACCOUNTS lifts it and keeps the saved list for next time.",
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [categoryValidation.SetCategoryVisibilitySchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      {
        status: 400,
        code: 'VALIDATION_FAILED',
        description: 'RESTRICTED with no accounts, or more than 500.',
      },
      { status: 404, code: 'NOT_FOUND', description: 'No such category.' },
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description: 'One of the accounts does not exist.',
      },
    ],
    conditionalPermissions: [
      {
        permission: 'CATALOG_MANAGE',
        when: 'Always. Without it the answer is 403.',
      },
    ],
    rateLimited: false,
    source:
      'src/app/api/v1/catalog/categories/[categoryId]/visibility/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/catalog/inventory/reconcile',
    tag: 'catalog',
    summary:
      'Its own path rather than another route under `catalog/products`: a stocktake is an operation on the…',
    description:
      'Its own path rather than another route under `catalog/products`: a stocktake is an operation on the warehouse, not on one product, and nesting it would have produced `/catalog/products/inventory/reconcile` — a path that reads as a product called "inventory". Per-product *adjustments* stay on the product, because those genuinely are about one product.\n\nAnswers 200, not 201: a stocktake creates nothing. It reports, and sometimes corrects. Every line comes back with its variance whether or not anything changed, because the discrepancy is the point of the exercise; `dryRun` reports without writing.',
    auth: 'bearer',
    permissions: [Permission.INVENTORY_MANAGE],
    roles: [],
    body: [productValidation.ReconcileStockSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/inventory/reconcile/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/catalog/products',
    tag: 'catalog',
    summary:
      'Scoped to what the caller may see: an administrator gets the whole catalogue including drafts…',
    description:
      'Scoped to what the caller may see: an administrator gets the whole catalogue including drafts, everyone else gets published products that are unrestricted or granted to their account.\n\nAsset URLs are omitted unless `withThumbnails` is set, which presigns one thumbnail per row; fetch a single product for links to every asset.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_VIEW],
    roles: [],
    body: [],
    query: [productValidation.ListProductsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/catalog/products',
    tag: 'catalog',
    summary:
      'Always lands in DRAFT — publishing is its own audited transition.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [productValidation.CreateProductSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/catalog/products/{productId}',
    tag: 'catalog',
    summary: 'One product, with options, variants and volume pricing.',
    description:
      "One product, with options, variants and volume pricing. A product the caller may not see is reported as missing rather than forbidden — saying a SKU exists but is not theirs leaks another customer's contract line.",
    auth: 'bearer',
    permissions: [Permission.CATALOG_VIEW],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/[productId]/route.ts',
  },
  {
    method: 'patch',
    path: '/api/v1/catalog/products/{productId}',
    tag: 'catalog',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [productValidation.UpdateProductSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/[productId]/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/catalog/products/{productId}',
    tag: 'catalog',
    summary: 'Only ever from DRAFT.',
    description:
      'Only ever from DRAFT. A published product is marked unavailable or superseded instead — orders and invoices reference it.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/[productId]/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/catalog/products/{productId}/assets',
    tag: 'catalog',
    summary: 'Attaches a library file.',
    description:
      'Names a file in the document library by `folderPath` and `fileName`; upload it with `POST /api/v1/dam/files` first. The portal copies the bytes into object storage and reads the filename, content type and size off the file itself, so there is no key, size or type for a caller to get wrong.\n\nAn IMAGE is recorded as PENDING a resize; until the worker writes the derivatives, reads serve the original in place of a thumbnail.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [productValidation.AttachAssetSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    errors: [
      {
        status: 404,
        code: 'NOT_FOUND',
        description: 'No file of that name in that library folder.',
      },
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description:
          'The library gave no link to the file, or it is empty or above the 50MB copy limit.',
      },
      {
        status: 503,
        code: 'DEPENDENCY_UNAVAILABLE',
        description:
          'The library is switched off, or DAM_SERVICE_LOGIN and DAM_SERVICE_PASSWORD are not configured.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/[productId]/assets/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/catalog/products/{productId}/assets/{assetId}',
    tag: 'catalog',
    summary: 'Removes the row first and the objects after.',
    description:
      'Removes the row first and the objects after. A failed object delete leaves an orphan for a sweeper to collect, which is strictly better than a row pointing at a file that no longer exists.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: false,
    source:
      'src/app/api/v1/catalog/products/[productId]/assets/[assetId]/route.ts',
  },
  {
    method: 'put',
    path: '/api/v1/catalog/products/{productId}/options',
    tag: 'catalog',
    summary: 'Replaces the whole option set.',
    description:
      'Replaces the whole option set. Refused when it would orphan a variant built on a value the new set no longer offers — the offending SKUs are named.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [productValidation.SetProductOptionsSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/[productId]/options/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/catalog/products/{productId}/status',
    tag: 'catalog',
    summary: 'The only way `status` ever changes.',
    description:
      'The only way `status` ever changes. See `product-status.ts` for the transition table — a published product never returns to DRAFT, and SUPERSEDED is terminal and must name its replacement.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [productValidation.ChangeProductStatusSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/[productId]/status/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/catalog/products/{productId}/stock',
    tag: 'catalog',
    summary:
      'A signed movement, never an absolute figure — two people counting the same shelf and both…',
    description:
      'A signed movement, never an absolute figure — two people counting the same shelf and both submitting "42" loses an adjustment; both submitting "+3" does not. A reason is required and lands in the audit trail.\n\nFor absolute counts see `POST /api/v1/catalog/inventory/reconcile`.',
    auth: 'bearer',
    permissions: [Permission.INVENTORY_MANAGE],
    roles: [],
    body: [productValidation.AdjustStockSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/[productId]/stock/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/catalog/products/{productId}/variants',
    tag: 'catalog',
    summary: 'Must choose a value for every option the product defines.',
    description:
      'Must choose a value for every option the product defines. Returns the whole product rather than the variant alone: the admin screen re-renders the variant matrix from it, and a bare variant would leave it stale.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [productValidation.CreateVariantSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/[productId]/variants/route.ts',
  },
  {
    method: 'patch',
    path: '/api/v1/catalog/products/{productId}/variants/{variantId}',
    tag: 'catalog',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [productValidation.UpdateVariantSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source:
      'src/app/api/v1/catalog/products/[productId]/variants/[variantId]/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/catalog/products/{productId}/variants/{variantId}',
    tag: 'catalog',
    summary: 'Soft — order lines reference the variant SKU.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: false,
    source:
      'src/app/api/v1/catalog/products/[productId]/variants/[variantId]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/catalog/products/{productId}/visibility',
    tag: 'catalog',
    summary:
      'The accounts a restricted product is open to, which `PUT` replaces wholesale and nothing else…',
    description:
      'The accounts a restricted product is open to, which `PUT` replaces wholesale and nothing else returns. Without it the panel that edits the allow-list had nothing to pre-fill from, so every save replaced the list with whatever was on screen.\n\nCATALOG_MANAGE rather than CATALOG_VIEW: this says which customers may buy a product, which is commercial information about other tenants and not part of browsing the catalogue.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      { status: 404, code: 'NOT_FOUND', description: 'No such product.' },
    ],
    conditionalPermissions: [
      {
        permission: 'CATALOG_MANAGE',
        when: 'Always. Without it the answer is 403.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/[productId]/visibility/route.ts',
  },
  {
    method: 'put',
    path: '/api/v1/catalog/products/{productId}/visibility',
    tag: 'catalog',
    summary:
      'Switching back to ALL_ACCOUNTS deliberately leaves the granted-account rows in place: a restriction…',
    description:
      'Switching back to ALL_ACCOUNTS deliberately leaves the granted-account rows in place: a restriction lifted for a campaign is usually reinstated, and re-entering fifty account ids by hand is how it gets reinstated wrongly.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [productValidation.SetVisibilitySchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/[productId]/visibility/route.ts',
  },
  {
    method: 'put',
    path: '/api/v1/catalog/products/{productId}/volume-tiers',
    tag: 'catalog',
    summary: 'Replaces the whole ladder.',
    description:
      'Replaces the whole ladder. PRICING_MANAGE rather than CATALOG_MANAGE: this is a price change, not a catalogue edit. Refused when a larger quantity would not discount more than a smaller one.',
    auth: 'bearer',
    permissions: [Permission.PRICING_MANAGE],
    roles: [],
    body: [productValidation.SetVolumeTiersSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/[productId]/volume-tiers/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/catalog/products/export',
    tag: 'catalog',
    summary:
      'The filtered catalogue as CSV (SOW M-13: "bulk import and export with template download").',
    description:
      "The filtered catalogue as CSV (SOW M-13: \"bulk import and export with template download\"). The editable columns come first, in the importer's own order, so the file can be corrected in a spreadsheet and imported back; the read-only tail — status, visibility and the two stock figures — is there to be read and is ignored on the way in.\n\nThe filters are the catalogue list's, minus paging, and are applied by the same function, so the file matches the screen it was exported from. Above the importer's per-file maximum the answer is 422 rather than a truncated file: a short catalogue export looks complete once it is open in Excel.\n\nCells that begin with a formula character are prefixed with an apostrophe, and the body carries a byte-order mark so Excel reads it as UTF-8 rather than as the system code page.",
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [],
    query: [productValidation.ExportProductsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    errors: [
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description: 'More products match than one export carries.',
      },
    ],
    conditionalPermissions: [
      {
        permission: 'CATALOG_MANAGE',
        when: 'Always. Without it the answer is 403.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/export/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/catalog/products/image-matches',
    tag: 'catalog',
    summary:
      'Says which product each image file name is for, by SKU, before a bulk image upload.',
    description:
      'Says which product each image file name is for, by SKU, before a bulk image upload. Nothing is written. The name is tried whole, then with up to three trailing segments cut off (`BC-001_2.jpg`, `BC-001-back.png`), and the longest SKU that exists wins. A `sku` sent with a file overrides its name.\n\nReturns one entry per file, in the order sent, with `product: null` for a file that matched nothing. Upload each matched file with `POST /api/v1/dam/files` and attach it with `POST /api/v1/catalog/products/:productId/assets`, using `nextImageSortOrder`.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [productValidation.MatchImageFilenamesSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/image-matches/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/catalog/products/import',
    tag: 'catalog',
    summary: 'Answers 202 with a job to poll.',
    description:
      'Answers 202 with a job to poll. The rows are stored and the work happens on the `import` queue, because a ten-thousand-row load has no business holding a request open — and in Next it would be holding open a process that is also rendering pages.\n\nRows are applied individually, so one bad row does not reject the file. Existing SKUs are skipped unless `updateExisting` is set. `dryRun` runs the same code without writing. Imported products land as DRAFT.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [productValidation.ImportProductsSchema],
    query: [],
    extraQuery: [],
    successStatuses: [202],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/import/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/catalog/products/import/jobs',
    tag: 'catalog',
    summary:
      'Recent runs, newest first, without the submitted rows or the per-row results — fetch one job for…',
    description:
      'Recent runs, newest first, without the submitted rows or the per-row results — fetch one job for those.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [],
    query: [productValidation.ListImportJobsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/import/jobs/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/catalog/products/import/jobs/{jobId}',
    tag: 'catalog',
    summary: 'QUEUED and RUNNING mean it is still going.',
    description:
      'QUEUED and RUNNING mean it is still going. COMPLETED means it finished — individual rows may still have failed, so read the counts. FAILED means the run itself broke and `error` says why. Includes the per-row results.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/import/jobs/[jobId]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/catalog/products/import/template',
    tag: 'catalog',
    summary:
      'The blank import file, with two worked rows (SOW M-13: "template download").',
    description:
      'The blank import file, with two worked rows (SOW M-13: "template download"). Its columns are `IMPORT_COLUMNS`, the same list the export writes first and `ImportRowSchema` reads, so the three cannot drift apart into a file the importer half understands.\n\nServed rather than kept as a static asset so it is built from that list at request time: a template checked into `public/` is a copy that goes stale the first time a column is added.',
    auth: 'bearer',
    permissions: [Permission.CATALOG_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [
      {
        permission: 'CATALOG_MANAGE',
        when: 'Always. Without it the answer is 403.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/catalog/products/import/template/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/dam/files',
    tag: 'dam',
    summary:
      "One folder's sub-folders and a page of its files, from Ticket-IT's…",
    description:
      "One folder's sub-folders and a page of its files, from Ticket-IT's `ImageManagement/GetFilesByClient`. Omit `folderPath` for the root.",
    auth: 'bearer',
    permissions: [Permission.DAM_VIEW],
    roles: [],
    body: [],
    query: [damValidation.DamFolderContentsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/dam/files/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/dam/files',
    tag: 'dam',
    summary: 'Adds files to the library.',
    description:
      '`multipart/form-data`: one or more file parts, plus an optional `folderPath` text part naming the folder to put them in. Omit it for the root. Ten files, 25MB each and 60MB in total at most; images and PDFs only.\n\nThe response carries one entry per file sent, each with the URL and folder the library now holds it under. Those come from re-listing the folder, because `UploadImage` itself answers with no URL and no id; an entry whose `confirmed` is false is a file that was stored but could not be found again, so its URL is null and the folder is worth a look.',
    auth: 'bearer',
    permissions: [Permission.DAM_UPLOAD],
    roles: [],
    body: [damValidation.DamUploadSchema],
    consumes: 'multipart/form-data',
    query: [],
    extraQuery: [],
    successStatuses: [201],
    errors: [
      {
        status: 400,
        code: 'VALIDATION_FAILED',
        description:
          'A file is too big, empty, badly named, or of a type the library does not take.',
      },
      {
        status: 403,
        code: 'TICKETIT_SESSION_REQUIRED',
        description:
          'The user has no live Ticket-IT session, so there is no library to write to.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/dam/files/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/dam/files',
    tag: 'dam',
    summary: 'Removes one file.',
    description:
      "Ticket-IT's `ImageManagement/UnlinkFile`. The target is named by folder and file name because that is a library file's whole identity — there are no ids — and `mimeType` is passed on when the caller knows it. Omit `folderPath` for a file in the root.\n\nPermanent: there is no undo upstream and the portal keeps no copy.",
    auth: 'bearer',
    permissions: [Permission.DAM_DELETE],
    roles: [],
    body: [],
    query: [damValidation.DamDeleteFileQuerySchema],
    extraQuery: [],
    successStatuses: [204],
    errors: [
      {
        status: 403,
        code: 'TICKETIT_SESSION_REQUIRED',
        description:
          'The user has no live Ticket-IT session, so there is no library to write to.',
      },
      {
        status: 404,
        code: 'NOT_FOUND',
        description: 'No such file in that folder.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/dam/files/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/dam/files/notes',
    tag: 'dam',
    summary:
      "The notes left against one file, from Ticket-IT's `ImageManagement/getFileNotes`.",
    description:
      "The notes left against one file, from Ticket-IT's `ImageManagement/getFileNotes`. Read-only: adding or deleting a note is not wired yet.",
    auth: 'bearer',
    permissions: [Permission.DAM_VIEW],
    roles: [],
    body: [],
    query: [damValidation.DamFileNotesQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/dam/files/notes/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/dam/folders',
    tag: 'dam',
    summary:
      "The client's folders, from Ticket-IT's `ImageManagement/GetFolderSubFolder`.",
    description:
      "The client's folders, from Ticket-IT's `ImageManagement/GetFolderSubFolder`. Pass a folder's `path` to /dam/files as `folderPath` to open it.",
    auth: 'bearer',
    permissions: [Permission.DAM_VIEW],
    roles: [],
    body: [],
    query: [damValidation.DamListQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/dam/folders/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/dam/folders',
    tag: 'dam',
    summary: 'Makes a folder.',
    description:
      "Ticket-IT's `ImageManagement/CreateFolder`. `folderPath` is the folder's full path, so a folder per template or product is one call rather than a walk down the tree.\n\nWhether a nested path creates its intermediate folders, and what happens when the folder already exists, are not documented upstream and are not guessed at here — Ticket-IT's own answer comes back. A caller that needs \"make it if it is missing\" checks GET /dam/folders first.",
    auth: 'bearer',
    permissions: [Permission.DAM_UPLOAD],
    roles: [],
    body: [damValidation.CreateDamFolderSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    errors: [
      {
        status: 403,
        code: 'TICKETIT_SESSION_REQUIRED',
        description:
          'The user has no live Ticket-IT session, so there is no library to write to.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/dam/folders/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/dam/images',
    tag: 'dam',
    summary:
      "Every image the client holds, across folders, from Ticket-IT's `ImageManagement/GetImagesByClient`.",
    auth: 'bearer',
    permissions: [Permission.DAM_VIEW],
    roles: [],
    body: [],
    query: [damValidation.DamListQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/dam/images/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/dam/search',
    tag: 'dam',
    summary: "Ticket-IT's `ImageManagement/AdvanceSearch`.",
    description:
      "Ticket-IT's `ImageManagement/AdvanceSearch`. Results can include folders as well as files; each item's `kind` says which.",
    auth: 'bearer',
    permissions: [Permission.DAM_VIEW],
    roles: [],
    body: [],
    query: [damValidation.DamListQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/dam/search/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/dam/status',
    tag: 'dam',
    summary:
      'Whether the image library can be offered to this user, answered without calling Ticket-IT.',
    description:
      'Whether the image library can be offered to this user, answered without calling Ticket-IT. `connected` means the portal holds a Ticket-IT session for them, not that Ticket-IT will still accept it — the first library call finds that out, and answers TICKETIT_SESSION_REQUIRED if not.',
    auth: 'bearer',
    permissions: [Permission.DAM_VIEW],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/dam/status/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/invitations',
    tag: 'invitations',
    summary: 'Outstanding and historical invitations.',
    auth: 'bearer',
    permissions: [Permission.USER_INVITE],
    roles: [],
    body: [],
    query: [invitationValidation.ListInvitationsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/invitations/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/invitations',
    tag: 'invitations',
    summary: 'The user row is created at *acceptance*, not here.',
    description:
      'The user row is created at *acceptance*, not here. A revoked or expired invitation therefore leaves nothing behind that could later be logged into, and an administrator who mistypes an address has not created an account for a stranger.\n\nInviting an address that already has a live invitation revokes the old one first: two valid tokens for one address means revoking one does not actually revoke access.',
    auth: 'bearer',
    permissions: [Permission.USER_INVITE],
    roles: [],
    body: [invitationValidation.CreateInvitationSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/invitations/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/invitations/{invitationId}/revoke',
    tag: 'invitations',
    summary:
      'An invitation that does not exist and one already accepted answer the same 404: distinguishing them…',
    description:
      'An invitation that does not exist and one already accepted answer the same 404: distinguishing them would leak whether an invitation was taken up.',
    auth: 'bearer',
    permissions: [Permission.USER_INVITE],
    roles: [],
    body: [],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [204],
    rateLimited: false,
    source: 'src/app/api/v1/invitations/[invitationId]/revoke/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/invitations/accept',
    tag: 'invitations',
    summary:
      'Public: the invitee has no account yet, which is the whole point.',
    description:
      'Public: the invitee has no account yet, which is the whole point. The account is discovered from the token rather than supplied by the caller.\n\nA wrong token, an expired one and an already-used one all answer identically — an invitee cannot act on the difference, and telling an attacker which they hit turns this into an oracle. Rate-limited for the same reason login is.\n\nSucceeds with a full session, so the new user lands signed in rather than at a login form typing the password they just chose.',
    auth: 'public',
    permissions: [],
    roles: [],
    body: [invitationValidation.AcceptInvitationSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: true,
    source: 'src/app/api/v1/invitations/accept/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/orders',
    tag: 'orders',
    summary:
      'Scoped by the widest order permission the caller holds: ORDER_VIEW_ACCOUNT sees the whole tenant…',
    description:
      'Scoped by the widest order permission the caller holds: ORDER_VIEW_ACCOUNT sees the whole tenant, ORDER_VIEW_SITE their branches, ORDER_VIEW_OWN only what they placed. An actor with none sees nothing — the filter fails closed.',
    auth: 'bearer',
    permissions: [Permission.ORDER_VIEW_OWN],
    roles: [],
    body: [],
    query: [orderValidation.ListOrdersQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/orders/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/orders',
    tag: 'orders',
    summary: 'Turns a validated basket into an order.',
    description:
      'Turns a validated basket into an order. The basket is re-validated here rather than trusted from an earlier call: a product can be unpublished, a rate card can expire and a budget can be consumed by a colleague between the review step and the submit, and the check that matters is the last one.\n\nEverything commits together — the order number, the lines, the stock reservation, the approval request and the basket closing.\n\nSafe to repeat. Send `cartId` (the `cart.id` the buyer reviewed): if that basket has already become an order — an earlier submit that succeeded, or one racing this one — that order is returned with 200 and nothing new is written. A 201 is a new order.',
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [orderValidation.PlaceOrderSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200, 201],
    errors: [
      {
        status: 409,
        code: 'CONFLICT',
        description:
          "The buyer's open basket is no longer the `cartId` sent — review it again.",
      },
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description:
          'The basket is not ready (`details.issues`), or delivery instructions are required (`DELIVERY_NOTES_REQUIRED`).',
      },
      {
        status: 503,
        code: 'TRANSACTION_ABORTED',
        description:
          'The placement ran out of time and was rolled back; nothing was saved. Retry, ideally with `cartId`.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/orders/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/orders/{orderId}',
    tag: 'orders',
    summary: 'One order with its lines and its full status history.',
    description:
      "One order with its lines and its full status history. An order the caller may not see answers 404, not 403 — telling a buyer that an order exists but is a colleague's leaks what other branches are spending on.",
    auth: 'bearer',
    permissions: [Permission.ORDER_VIEW_OWN],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/orders/[orderId]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/orders/{orderId}/docket',
    tag: 'orders',
    summary:
      "The fulfilment docket as a PDF: the order's lines with their options, personalised wording and line…",
    description:
      "The fulfilment docket as a PDF: the order's lines with their options, personalised wording and line notes, and the order notes and delivery instructions at the top (SOW F-19). Readable by whoever may see the order.",
    auth: 'bearer',
    permissions: [Permission.ORDER_VIEW_OWN],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    produces: 'application/pdf',
    rateLimited: false,
    source: 'src/app/api/v1/orders/[orderId]/docket/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/orders/{orderId}/lines/{lineId}/preview',
    tag: 'orders',
    summary:
      'The picture of a personalised design as the buyer finished it — the artwork an approver is deciding…',
    description:
      "The picture of a personalised design as the buyer finished it — the artwork an approver is deciding on. Readable by anyone who may see the order, and by an approver in its account while it has an approval request. 404 when the line has no preview; the approval view's `image` says which lines do.\n\nRaster types only, sent with `nosniff` and a CSP that allows nothing, because the bytes were supplied by a buyer.",
    auth: 'bearer',
    permissions: [Permission.ORDER_VIEW_OWN],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    produces: 'application/octet-stream',
    rateLimited: false,
    source: 'src/app/api/v1/orders/[orderId]/lines/[lineId]/preview/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/orders/{orderId}/payment',
    tag: 'orders',
    summary:
      'Payment moves on its own axis, which is why it is not a status change: an order can be DELIVERED…',
    description:
      'Payment moves on its own axis, which is why it is not a status change: an order can be DELIVERED and UNPAID on Net 30 terms, and PAID while still PROCESSING on a P-Card.\n\nBILLING_VIEW opens the route so the order can be read; writing the payment additionally needs BILLING_MANAGE, checked in the service.',
    auth: 'bearer',
    permissions: [Permission.BILLING_VIEW],
    roles: [],
    body: [orderValidation.RecordPaymentSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    conditionalPermissions: [
      {
        permission: 'BILLING_MANAGE',
        when: 'Always, to record the payment. Without it the answer is 403.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/orders/[orderId]/payment/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/orders/{orderId}/reorder',
    tag: 'orders',
    summary: 'One-click re-order (SOW M-08).',
    description:
      "One-click re-order (SOW M-08). Puts the order's lines back in the basket with their specifications intact — product, configuration, quantity, the design and the personalisation printed on it — and answers with the basket and a row per original line saying what became of it.\n\nPrices are not carried. The lines are priced today, through the ordinary basket, because the price on an order is the price it was placed at and a basket quoting last year's figure would not survive checkout.\n\nNothing is substituted. A line goes back as it was or not at all, and each row says why not with a `reasonCode` a screen can act on: PRODUCT_WITHDRAWN, PRODUCT_REPLACED (with `replacedBy` naming the successor, rather than the design being moved onto a product it was not drawn for), DESIGN_WITHDRAWN, DESIGN_REQUIRED (an order from before template pricing — the item is still on sale, it needs a design), or REFUSED with the basket's own words.\n\nThe basket is added to, never replaced: a re-order must not discard work already in progress.\n\nBoth permissions, not either: re-ordering is reading an order and filling a basket, and holding one without the other is not enough to do it. An order the caller may not see answers 404, as the order page does.",
    auth: 'bearer',
    permissions: [Permission.ORDER_VIEW_OWN, Permission.ORDER_CREATE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    errors: [
      {
        status: 404,
        code: 'NOT_FOUND',
        description: 'No such order, or not one this caller may see.',
      },
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description:
          'Not one line could be ordered again; `details.lines` says why for each.',
      },
    ],
    conditionalPermissions: [
      {
        permission: 'ORDER_VIEW_OWN',
        when: 'Always. Narrower roles see only their own orders.',
      },
      {
        permission: 'ORDER_CREATE',
        when: 'Always. Without it the answer is 403.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/orders/[orderId]/reorder/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/orders/{orderId}/shipments',
    tag: 'orders',
    summary: 'The NZ Post labels made for this order.',
    description:
      'Voided and failed ones included, oldest first, each with its parcels and tracking references.',
    auth: 'bearer',
    permissions: [Permission.ORDER_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/orders/[orderId]/shipments/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/orders/{orderId}/shipments',
    tag: 'orders',
    summary: 'Request an NZ Post label for a packed order.',
    description:
      "The order must be PROCESSING. Send each box's weight and size as measured; the service defaults to the one the buyer chose, and the delivery address to the one they validated at checkout — send `deliveryAddress` for an order that has none.\n\nAnswers 201 with the shipment PENDING; the label is made in the background and the shipment becomes LABELLED, or FAILED with the reason. Poll `GET /orders/:orderId/shipments/:shipmentId`. One live label per order: void the existing one first. Resending with the same `idempotencyKey` answers 200 with the shipment already made.",
    auth: 'bearer',
    permissions: [Permission.ORDER_MANAGE],
    roles: [],
    body: [shippingValidation.CreateShipmentSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200, 201],
    errors: [
      {
        status: 409,
        code: 'CONFLICT',
        description:
          'The order already has a live label — void it first — or the `idempotencyKey` was used for a different order.',
      },
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description:
          'The order is not PROCESSING, has no validated delivery address and none was sent, or no service could be chosen.',
      },
      {
        status: 503,
        code: 'DEPENDENCY_UNAVAILABLE',
        description:
          'Shipping is switched off, or live labels are not configured (see `GET /shipping/status`).',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/orders/[orderId]/shipments/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/orders/{orderId}/shipments/{shipmentId}',
    tag: 'orders',
    summary: 'One label request and where it is.',
    description:
      '`status` moves PENDING → SUBMITTED → LABELLED, or ends FAILED with `lastError`. `labelReady` turns true when the PDF can be downloaded.',
    auth: 'bearer',
    permissions: [Permission.ORDER_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/orders/[orderId]/shipments/[shipmentId]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/orders/{orderId}/shipments/{shipmentId}/label',
    tag: 'orders',
    summary: 'A short-lived link to the label PDF.',
    description:
      "A presigned URL, valid for S3_PRESIGN_EXPIRY_SECONDS. Every download is recorded in the audit trail: a label carries the recipient's name, phone and address. Answers 422 while the label is still being made.",
    auth: 'bearer',
    permissions: [Permission.ORDER_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description:
          'The label is not ready yet (`details.status`, `details.lastError`).',
      },
    ],
    rateLimited: false,
    source:
      'src/app/api/v1/orders/[orderId]/shipments/[shipmentId]/label/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/orders/{orderId}/shipments/{shipmentId}/retry',
    tag: 'orders',
    summary: 'Replay a failed label request.',
    description:
      'Only a FAILED shipment, only while the order is in production. The stored request is sent again; a shipment that already has a consignment id resumes from fetching its label instead of asking NZ Post for a new consignment.',
    auth: 'bearer',
    permissions: [Permission.ORDER_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    errors: [
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description:
          'The shipment is not FAILED, or the order is no longer in production.',
      },
    ],
    rateLimited: false,
    source:
      'src/app/api/v1/orders/[orderId]/shipments/[shipmentId]/retry/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/orders/{orderId}/shipments/{shipmentId}/void',
    tag: 'orders',
    summary: 'Withdraw a label in the portal.',
    description:
      'NZ Post has no void endpoint. This stops the portal treating the label as live — dispatch and pickups ignore it — and whoever printed it discards the copy. A label nobody scans is never charged.\n\nRefused once the order has been dispatched on this label. Voiding an already voided label answers with it unchanged.',
    auth: 'bearer',
    permissions: [Permission.ORDER_MANAGE],
    roles: [],
    body: [shippingValidation.VoidShipmentSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    errors: [
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description: 'The order has already been dispatched on this label.',
      },
    ],
    rateLimited: false,
    source:
      'src/app/api/v1/orders/[orderId]/shipments/[shipmentId]/void/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/orders/{orderId}/status',
    tag: 'orders',
    summary:
      'The state machine decides what is *possible*; a second check decides who is *allowed*.',
    description:
      "The state machine decides what is *possible*; a second check decides who is *allowed*. Approval decisions need APPROVAL_ACT, fulfilment moves need ORDER_MANAGE, and a buyer may always cancel their own order.\n\nThe shelf moves in the same transaction as the status: dispatching consumes the reservation, rejecting or cancelling releases it.\n\nAn order routed by approval rules is decided on its approval step: APPROVED, REJECTED or CHANGES_REQUESTED here records the caller's decision on the step open at the current tier, exactly as `POST /approvals/steps/{stepId}` would. On a multi-tier order that can leave the order PENDING_APPROVAL — read `status` from the response. `deliveryNotes`, when sent, replaces the order's delivery instructions.",
    auth: 'bearer',
    permissions: [Permission.ORDER_VIEW_OWN],
    roles: [],
    body: [orderValidation.ChangeOrderStatusSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    errors: [
      {
        status: 403,
        code: 'FORBIDDEN',
        description:
          'A permission above is missing, or the order is waiting on approval steps and none open now is addressed to the caller (`details.openSteps`).',
      },
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description:
          'The move is not allowed from the current status (`details.allowed`), or a dispatch has no NZ Post label and no carrier and tracking number.',
      },
    ],
    conditionalPermissions: [
      {
        permission: 'APPROVAL_ACT',
        when: 'To move to APPROVED, REJECTED or CHANGES_REQUESTED. The person who placed the order cannot decide it.',
      },
      {
        permission: 'ORDER_MANAGE',
        when: 'To move to PROCESSING, DISPATCHED or DELIVERED.',
      },
      {
        permission: 'ORDER_CANCEL',
        when: 'To cancel an order someone else placed. A buyer may always cancel their own.',
      },
      {
        permission: 'ORDER_CREATE',
        when: 'To resubmit for approval an order someone else placed. A buyer may always resubmit their own.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/orders/[orderId]/status/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/orders/{orderId}/tracking',
    tag: 'orders',
    summary: "The order's NZ Post tracking timeline.",
    description:
      'What the portal holds from ParcelTrack, newest event first. Fast whatever NZ Post is doing: nothing is fetched while the request waits. When what is held is more than half an hour old and the order is dispatched, a refresh is queued in the background and `refreshQueued` says so.\n\nVisible to whoever may see the order — the same rule as `GET /orders/:orderId`.',
    auth: 'bearer',
    permissions: [Permission.ORDER_VIEW_OWN],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/orders/[orderId]/tracking/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/orders/{orderId}/tracking/refresh',
    tag: 'orders',
    summary: 'Poll ParcelTrack for this order now.',
    description:
      'For an operator looking into a delivery. Waits for NZ Post and answers with how many new events arrived; if every parcel is now delivered, the order moves to DELIVERED as it would on a scheduled poll.',
    auth: 'bearer',
    permissions: [Permission.ORDER_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    errors: [
      {
        status: 503,
        code: 'DEPENDENCY_UNAVAILABLE',
        description:
          'Shipping is switched off, tracking is not configured, or NZ Post could not be reached.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/orders/[orderId]/tracking/refresh/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/password/change',
    tag: 'password',
    summary:
      'Proves the old credential first, so a token lifted from an unlocked machine cannot be used to take…',
    description:
      'Proves the old credential first, so a token lifted from an unlocked machine cannot be used to take the account over. "No local password" and "wrong password" answer identically: distinguishing them would tell an attacker holding a stolen token which accounts are legacy-backed.\n\nEvery refresh token is revoked, so no session can be renewed. Access tokens are stateless and are *not* revoked — one already issued keeps working for up to fifteen minutes, which is worth stating because "signs you out everywhere" is what a user will assume.',
    auth: 'bearer',
    permissions: [],
    roles: [],
    body: [invitationValidation.ChangePasswordSchema],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: false,
    source: 'src/app/api/v1/password/change/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/password/forgot',
    tag: 'password',
    summary: 'Always 204, whatever the identifier was.',
    description:
      'Always 204, whatever the identifier was. An endpoint that answered differently for a known and an unknown address would let anyone test whether a given person has an account here.\n\nA user whose account lives in Ticket-IT is forwarded to it: their password is upstream, so Ticket-IT sends the email and mints the token, and the portal only relays the request. An identifier with no local user at all is forwarded too — someone who has never signed in here still has an account there.',
    auth: 'public',
    permissions: [],
    roles: [],
    body: [invitationValidation.RequestPasswordResetSchema],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: true,
    source: 'src/app/api/v1/password/forgot/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/password/reset',
    tag: 'password',
    summary: 'Completes a reset and revokes every session.',
    description:
      'Completes a reset and revokes every session. A password is usually reset because the old one may be known to someone else, and leaving their refresh token live for another thirty days would make the reset cosmetic.',
    auth: 'public',
    permissions: [],
    roles: [],
    body: [invitationValidation.ResetPasswordSchema],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: true,
    source: 'src/app/api/v1/password/reset/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/pricing/active-rate-card',
    tag: 'pricing',
    summary: 'The rate card pricing this account right now.',
    description:
      'The rate card pricing this account right now. Null when there is no contract in force, which is the ordinary case rather than an error. Administrators may pass `accountId` to ask on behalf of a customer.',
    auth: 'bearer',
    permissions: [Permission.PRICING_VIEW],
    roles: [],
    body: [],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/pricing/active-rate-card/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/pricing/quote',
    tag: 'pricing',
    summary: 'Prices a batch of lines for this account.',
    description:
      'Prices a batch of lines for this account. Batched because a product grid needs every tile priced at once. Each line comes back with the contract price, the catalogue price it is measured against, the rule that produced it (`source`) and the ladder as this account sees it. Products the caller may not see are omitted rather than raising, so one unpublished SKU cannot blank a page.\n\n`at` and `accountId` are administrator-only and enforced in the service: a customer is always quoted against their own account, as of now.\n\nA quote writes nothing. POST because the request carries a list of lines that would not survive a query string, and 200 rather than 201 because nothing was created.',
    auth: 'bearer',
    permissions: [Permission.PRICING_VIEW],
    roles: [],
    body: [rateCardValidation.QuoteSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/pricing/quote/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/pricing/rate-cards',
    tag: 'pricing',
    summary: 'PRICING_VIEW, so a customer can see their own contracts.',
    description:
      'PRICING_VIEW, so a customer can see their own contracts. A customer is pinned to their own account before the query is built; only an administrator gets the cross-tenant list the pricing admin screen needs.',
    auth: 'bearer',
    permissions: [Permission.PRICING_VIEW],
    roles: [],
    body: [],
    query: [rateCardValidation.ListRateCardsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/pricing/rate-cards/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/pricing/rate-cards',
    tag: 'pricing',
    summary: 'Always lands in DRAFT.',
    description:
      'Always lands in DRAFT. Activation is its own transition, and it is there that the "one active card per account per period" rule is arbitrated.',
    auth: 'bearer',
    permissions: [Permission.PRICING_MANAGE],
    roles: [],
    body: [rateCardValidation.CreateRateCardSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/pricing/rate-cards/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/pricing/rate-cards/{rateCardId}',
    tag: 'pricing',
    summary:
      'A card belonging to another account is reported as missing rather than forbidden: confirming that a…',
    description:
      'A card belonging to another account is reported as missing rather than forbidden: confirming that a contract exists for another company is itself a disclosure.',
    auth: 'bearer',
    permissions: [Permission.PRICING_VIEW],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/pricing/rate-cards/[rateCardId]/route.ts',
  },
  {
    method: 'patch',
    path: '/api/v1/pricing/rate-cards/{rateCardId}',
    tag: 'pricing',
    summary:
      'ACTIVE cards are editable on purpose — a price correction on a live contract is an ordinary thing…',
    description:
      'ACTIVE cards are editable on purpose — a price correction on a live contract is an ordinary thing to need. ARCHIVED ones are not.',
    auth: 'bearer',
    permissions: [Permission.PRICING_MANAGE],
    roles: [],
    body: [rateCardValidation.UpdateRateCardSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/pricing/rate-cards/[rateCardId]/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/pricing/rate-cards/{rateCardId}',
    tag: 'pricing',
    summary: 'Soft: orders priced under this card reference it.',
    description:
      'Soft: orders priced under this card reference it. Archiving on the way out also releases its slot in the overlap constraint, so a deleted card does not keep blocking its successor.',
    auth: 'bearer',
    permissions: [Permission.PRICING_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: false,
    source: 'src/app/api/v1/pricing/rate-cards/[rateCardId]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/pricing/rate-cards/{rateCardId}/items',
    tag: 'pricing',
    summary:
      'A negotiated price list runs to hundreds of lines, so the detail screen pages through them here…',
    description:
      "A negotiated price list runs to hundreds of lines, so the detail screen pages through them here rather than pulling all of them on every card read. `effectivePrice` is the unit price at the product's minimum order quantity, and `source` says which rule produced it.",
    auth: 'bearer',
    permissions: [Permission.PRICING_VIEW],
    roles: [],
    body: [],
    query: [rateCardValidation.ListRateCardItemsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/pricing/rate-cards/[rateCardId]/items/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/pricing/rate-cards/{rateCardId}/items',
    tag: 'pricing',
    summary:
      'Bulk editor: upserts the named products and, with `replaceAll`, removes everything else.',
    description:
      'Bulk editor: upserts the named products and, with `replaceAll`, removes everything else. The whole payload is one transaction — half a price list is worse than none, because the half that landed would start pricing orders immediately.',
    auth: 'bearer',
    permissions: [Permission.PRICING_MANAGE],
    roles: [],
    body: [rateCardValidation.SetRateCardItemsSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/pricing/rate-cards/[rateCardId]/items/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/pricing/rate-cards/{rateCardId}/items/{productId}',
    tag: 'pricing',
    summary:
      "Removes one negotiated line, so the product falls back to the card's default discount — or to the…",
    description:
      "Removes one negotiated line, so the product falls back to the card's default discount — or to the catalogue price when the card sets none.",
    auth: 'bearer',
    permissions: [Permission.PRICING_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: false,
    source:
      'src/app/api/v1/pricing/rate-cards/[rateCardId]/items/[productId]/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/pricing/rate-cards/{rateCardId}/status',
    tag: 'pricing',
    summary:
      'DRAFT → ACTIVE → ARCHIVED, and DRAFT → ARCHIVED for a card that was never signed.',
    description:
      'DRAFT → ACTIVE → ARCHIVED, and DRAFT → ARCHIVED for a card that was never signed. Activating is the interesting one: a database EXCLUDE constraint refuses a second card whose window overlaps an already-active one for the same account, and that rejection comes back as a 409 explaining it rather than as a 500.',
    auth: 'bearer',
    permissions: [Permission.PRICING_MANAGE],
    roles: [],
    body: [rateCardValidation.ChangeRateCardStatusSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/pricing/rate-cards/[rateCardId]/status/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/approvals/activity',
    tag: 'reports',
    summary:
      'Every approval decision in a window, by approver and outcome, with the cycle time from submission…',
    description:
      'Every approval decision in a window, by approver and outcome, with the cycle time from submission to decision (SOW §15: "approval activity — by approver, outcome and date range — governance and approval cycle time").\n\nCovers both routes an order takes to an approver: rule steps, one row per tier decided, and account-threshold holds, released through the order\'s own status. The two are read so that no decision is counted twice. Answers with totals, a row per approver, and the decisions themselves, newest first.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ApprovalActivityQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      {
        status: 403,
        code: 'FORBIDDEN',
        description:
          'A non-administrator naming an account other than their own.',
      },
    ],
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/approvals/activity/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/approvals/activity-by-approver.csv',
    tag: 'reports',
    summary:
      'Decisions per approver, by outcome, with average and median hours to decide.',
    description:
      'Decisions per approver, by outcome, with average and median hours to decide.\n\nThe same rows as `GET /api/v1/reports/approvals/activity-by-approver`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ApprovalActivityQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source:
      'src/app/api/v1/reports/approvals/activity-by-approver.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/approvals/activity-by-approver.xlsx',
    tag: 'reports',
    summary:
      'Decisions per approver, by outcome, with average and median hours to decide.',
    description:
      'Decisions per approver, by outcome, with average and median hours to decide.\n\nThe same rows as `GET /api/v1/reports/approvals/activity-by-approver`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ApprovalActivityQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source:
      'src/app/api/v1/reports/approvals/activity-by-approver.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/approvals/activity.csv',
    tag: 'reports',
    summary:
      'Every approval decision in the window: who, which order, the outcome and the cycle time.',
    description:
      'Every approval decision in the window: who, which order, the outcome and the cycle time.\n\nThe same rows as `GET /api/v1/reports/approvals/activity`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ApprovalActivityQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/approvals/activity.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/approvals/activity.xlsx',
    tag: 'reports',
    summary:
      'Every approval decision in the window: who, which order, the outcome and the cycle time.',
    description:
      'Every approval decision in the window: who, which order, the outcome and the cycle time.\n\nThe same rows as `GET /api/v1/reports/approvals/activity`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ApprovalActivityQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/approvals/activity.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/dashboard',
    tag: 'reports',
    summary:
      'Every card and chart on a dashboard, in one response — the range is resolved once, so no card can…',
    description:
      'Every card and chart on a dashboard, in one response — the range is resolved once, so no card can disagree with the chart beside it.\n\n`spend`, `trend`, `byStatus`, `pace` and `topSites` answer "what happened in the window"; `queue` and `network` are a snapshot of now. That asymmetry is deliberate: an order placed six weeks ago and still in production is work somebody is waiting on today, and a windowed queue would hide it.\n\n`scope=platform` is administrator-only and is refused in the service.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.DashboardQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/reports/dashboard/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/dashboard/pdf',
    tag: 'reports',
    summary:
      'The executive spend dashboard as a PDF (SOW §15: "PDF for the consolidated billing summary and…',
    description:
      'The executive spend dashboard as a PDF (SOW §15: "PDF for the consolidated billing summary and executive dashboards"; the billing summary is the invoice PDF). Takes the same filters as `GET /reports/dashboard` and renders that response as it is — headline figures with the comparison period, the spend trend, orders by status, the work-in-progress snapshot and the top branches — so it carries the figures the screen shows and no others.\n\n`scope=platform` is administrator-only, refused in the service as on the JSON route.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.DashboardQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'application/pdf',
    errors: [
      {
        status: 403,
        code: 'FORBIDDEN',
        description: '`scope=platform` from anyone but an administrator.',
      },
    ],
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/dashboard/pdf/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/inventory',
    tag: 'reports',
    summary:
      'The warehouse view: what is low, what is out, and what to reorder.',
    description:
      'The warehouse view: what is low, what is out, and what to reorder. Healthy lines are trimmed — this is a "what needs attention" report, and a thousand rows of nothing wrong is not one.\n\nINVENTORY_MANAGE rather than INVENTORY_VIEW, and that is the whole protection: this report spans the global catalogue and is deliberately *not* filtered by product visibility, because a reorder list has to include lines restricted to one customer\'s contract. Head office holds INVENTORY_VIEW and would otherwise see them.',
    auth: 'bearer',
    permissions: [Permission.INVENTORY_MANAGE],
    roles: [],
    body: [],
    query: [reportValidation.InventoryQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/reports/inventory/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/inventory.csv',
    tag: 'reports',
    summary:
      'What is low, what is out, and what to reorder — the warehouse view.',
    description:
      'What is low, what is out, and what to reorder — the warehouse view.\n\nThe same rows as `GET /api/v1/reports/inventory`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.INVENTORY_MANAGE],
    roles: [],
    body: [],
    query: [reportValidation.InventoryQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [
      { permission: 'INVENTORY_MANAGE', when: 'Always.' },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/reports/inventory.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/inventory.xlsx',
    tag: 'reports',
    summary:
      'What is low, what is out, and what to reorder — the warehouse view.',
    description:
      'What is low, what is out, and what to reorder — the warehouse view.\n\nThe same rows as `GET /api/v1/reports/inventory`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.INVENTORY_MANAGE],
    roles: [],
    body: [],
    query: [reportValidation.InventoryQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [
      { permission: 'INVENTORY_MANAGE', when: 'Always.' },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/reports/inventory.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/inventory/turnover',
    tag: 'reports',
    summary: 'How fast stock is moving relative to what is held.',
    description:
      'How fast stock is moving relative to what is held. Units shipped count DISPATCHED and DELIVERED orders only — goods still in production have not left the shelf, and counting them would overstate movement in exactly the month a large order was placed and not yet filled.\n\nA product that shipped nothing still appears, with a zero: "what is not moving" is half the question this answers.\n\nAdministrator-only for the same reason the warehouse report is — it spans the whole global catalogue.',
    auth: 'bearer',
    permissions: [Permission.INVENTORY_MANAGE],
    roles: [],
    body: [],
    query: [
      reportValidation.ReportRangeQuerySchema,
      reportValidation.InventoryQuerySchema,
    ],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/reports/inventory/turnover/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/inventory/turnover.csv',
    tag: 'reports',
    summary: 'Units shipped against stock on hand, with days of cover.',
    description:
      'Units shipped against stock on hand, with days of cover.\n\nThe same rows as `GET /api/v1/reports/inventory/turnover`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.INVENTORY_MANAGE],
    roles: [],
    body: [],
    query: [
      reportValidation.ReportRangeQuerySchema,
      reportValidation.InventoryQuerySchema,
    ],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [
      { permission: 'INVENTORY_MANAGE', when: 'Always.' },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/reports/inventory/turnover.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/inventory/turnover.xlsx',
    tag: 'reports',
    summary: 'Units shipped against stock on hand, with days of cover.',
    description:
      'Units shipped against stock on hand, with days of cover.\n\nThe same rows as `GET /api/v1/reports/inventory/turnover`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.INVENTORY_MANAGE],
    roles: [],
    body: [],
    query: [
      reportValidation.ReportRangeQuerySchema,
      reportValidation.InventoryQuerySchema,
    ],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [
      { permission: 'INVENTORY_MANAGE', when: 'Always.' },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/reports/inventory/turnover.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/orders/ageing',
    tag: 'reports',
    summary:
      'Every open order and how long it has sat in its current status (SOW §15, order operations…',
    description:
      'Every open order and how long it has sat in its current status (SOW §15, order operations dashboard: "orders by status, awaiting approval, in production, in transit, and ageing"). Counted per status into bands — 0–2, 3–7, 8–14, 15–30 and 31+ days — with the orders themselves listed longest-waiting first.\n\nDays in the current status, not since the order was placed: an order approved ten days ago and still not in production is the one to chase, and age since placement would rank it below one placed a month ago and dispatched yesterday. Both figures are on every row.\n\nA snapshot as of now; there is no date range, because a window would hide exactly the old orders this exists to find.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.OrderAgeingQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      {
        status: 403,
        code: 'FORBIDDEN',
        description:
          'A non-administrator naming an account other than their own.',
      },
    ],
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/orders/ageing/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/orders/ageing.csv',
    tag: 'reports',
    summary:
      'Every open order and how long it has sat in its status, longest first.',
    description:
      'Every open order and how long it has sat in its status, longest first.\n\nThe same rows as `GET /api/v1/reports/orders/ageing`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.OrderAgeingQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/orders/ageing.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/orders/ageing.xlsx',
    tag: 'reports',
    summary:
      'Every open order and how long it has sat in its status, longest first.',
    description:
      'Every open order and how long it has sat in its status, longest first.\n\nThe same rows as `GET /api/v1/reports/orders/ageing`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.OrderAgeingQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/orders/ageing.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/orders/by-status',
    tag: 'reports',
    summary: 'Where work is stuck.',
    description:
      'Where work is stuck. Unlike the spend reports this counts *every* status, rejected and cancelled included — the ones carrying no money are often the interesting ones.\n\nShares are of the order count, not of the money: a single large order would otherwise make a busy queue look empty.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/reports/orders/by-status/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/orders/by-status.csv',
    tag: 'reports',
    summary: 'Orders and their value at each status.',
    description:
      'Orders and their value at each status.\n\nThe same rows as `GET /api/v1/reports/orders/by-status`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/orders/by-status.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/orders/by-status.xlsx',
    tag: 'reports',
    summary: 'Orders and their value at each status.',
    description:
      'Orders and their value at each status.\n\nThe same rows as `GET /api/v1/reports/orders/by-status`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/orders/by-status.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/orders/history',
    tag: 'reports',
    summary:
      'Who ordered what, for which branch, when, how many and for how much (SOW §15 "order history / audit…',
    description:
      'Who ordered what, for which branch, when, how many and for how much (SOW §15 "order history / audit extract — by account, site, user, status and date range"; AD-7). One row per order line, newest order first, paged.\n\nThe values are each order\'s own snapshot — the price it was placed at — not today\'s catalogue. Drafts are never included. The `.csv` and `.xlsx` routes beside this one carry the whole window rather than a page.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.OrderHistoryQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      {
        status: 403,
        code: 'FORBIDDEN',
        description:
          'A non-administrator naming an account other than their own.',
      },
    ],
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/orders/history/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/orders/history.csv',
    tag: 'reports',
    summary:
      'Who ordered what, for which branch, when, how many and for how much — one row per order line.',
    description:
      'Who ordered what, for which branch, when, how many and for how much — one row per order line.\n\nThe same rows as `GET /api/v1/reports/orders/history`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.OrderHistoryQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    errors: [
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description:
          'More lines match than one export carries; narrow the filters.',
      },
    ],
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/orders/history.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/orders/history.xlsx',
    tag: 'reports',
    summary:
      'Who ordered what, for which branch, when, how many and for how much — one row per order line.',
    description:
      'Who ordered what, for which branch, when, how many and for how much — one row per order line.\n\nThe same rows as `GET /api/v1/reports/orders/history`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.OrderHistoryQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    errors: [
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description:
          'More lines match than one export carries; narrow the filters.',
      },
    ],
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/orders/history.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/orders/velocity',
    tag: 'reports',
    summary: 'How fast orders are arriving, rather than what they are worth.',
    description:
      'How fast orders are arriving, rather than what they are worth. A month of many small orders and a month of one large one look identical on a revenue chart and mean very different things to a production team.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/reports/orders/velocity/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/orders/velocity.csv',
    tag: 'reports',
    summary: 'Orders per period — the velocity chart as rows.',
    description:
      'Orders per period — the velocity chart as rows.\n\nThe same rows as `GET /api/v1/reports/orders/velocity`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/orders/velocity.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/orders/velocity.xlsx',
    tag: 'reports',
    summary: 'Orders per period — the velocity chart as rows.',
    description:
      'Orders per period — the velocity chart as rows.\n\nThe same rows as `GET /api/v1/reports/orders/velocity`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/orders/velocity.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/products/top',
    tag: 'reports',
    summary:
      'The SKUs accounting for the most spend, or the most units with `by=quantity`.',
    description:
      'The SKUs accounting for the most spend, or the most units with `by=quantity`.\n\nGrouped on the snapshotted sku and name rather than the live product, so a product renamed since does not split its own history in two.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.TopProductsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/reports/products/top/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/products/top.csv',
    tag: 'reports',
    summary: 'The most-ordered SKUs, by spend or by quantity.',
    description:
      'The most-ordered SKUs, by spend or by quantity.\n\nThe same rows as `GET /api/v1/reports/products/top`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.TopProductsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/products/top.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/products/top.xlsx',
    tag: 'reports',
    summary: 'The most-ordered SKUs, by spend or by quantity.',
    description:
      'The most-ordered SKUs, by spend or by quantity.\n\nThe same rows as `GET /api/v1/reports/products/top`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.TopProductsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/products/top.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/by-account',
    tag: 'reports',
    summary:
      'Spend per customer account — the one genuinely cross-tenant report, which is why it runs outside…',
    description:
      'Spend per customer account — the one genuinely cross-tenant report, which is why it runs outside the tenant scope and why ACCOUNT_MANAGE guards it. That permission is administrator-only, and this must never be reachable without it.',
    auth: 'bearer',
    permissions: [Permission.ACCOUNT_MANAGE],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/by-account/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/by-account.csv',
    tag: 'reports',
    summary: 'Spend per customer account, across the platform.',
    description:
      'Spend per customer account, across the platform.\n\nThe same rows as `GET /api/v1/reports/spend/by-account`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.ACCOUNT_MANAGE],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [{ permission: 'ACCOUNT_MANAGE', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/by-account.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/by-account.xlsx',
    tag: 'reports',
    summary: 'Spend per customer account, across the platform.',
    description:
      'Spend per customer account, across the platform.\n\nThe same rows as `GET /api/v1/reports/spend/by-account`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.ACCOUNT_MANAGE],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [{ permission: 'ACCOUNT_MANAGE', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/by-account.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/by-category',
    tag: 'reports',
    summary: 'Spend per product category.',
    description:
      'Spend per product category. Raw SQL, because the grouping column is two joins away — the tenant scope still applies, since RLS is enforced by the database rather than by the query builder.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/by-category/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/by-category.csv',
    tag: 'reports',
    summary:
      "Spend per product category, with each category's share of the window.",
    description:
      'Spend per product category, with each category\'s share of the window.\n\nThe same rows as `GET /api/v1/reports/spend/by-category`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/by-category.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/by-category.xlsx',
    tag: 'reports',
    summary:
      "Spend per product category, with each category's share of the window.",
    description:
      'Spend per product category, with each category\'s share of the window.\n\nThe same rows as `GET /api/v1/reports/spend/by-category`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/by-category.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/by-region',
    tag: 'reports',
    summary:
      "Spend by delivery region, read from each order's frozen shipping snapshot rather than from the…",
    description:
      "Spend by delivery region, read from each order's frozen shipping snapshot rather than from the address row: an address corrected later must not move historical spend between regions.\n\nOrders with no region recorded are grouped rather than dropped, so the total still reconciles with the headline.",
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/by-region/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/by-region.csv',
    tag: 'reports',
    summary: 'Spend per region.',
    description:
      'Spend per region.\n\nThe same rows as `GET /api/v1/reports/spend/by-region`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/by-region.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/by-region.xlsx',
    tag: 'reports',
    summary: 'Spend per region.',
    description:
      'Spend per region.\n\nThe same rows as `GET /api/v1/reports/spend/by-region`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/by-region.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/by-site',
    tag: 'reports',
    summary:
      "Spend per branch, biggest first, each with its share of the window's total.",
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/by-site/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/by-site.csv',
    tag: 'reports',
    summary:
      "Spend per branch, biggest first, with each branch's share of the window.",
    description:
      'Spend per branch, biggest first, with each branch\'s share of the window.\n\nThe same rows as `GET /api/v1/reports/spend/by-site`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/by-site.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/by-site.xlsx',
    tag: 'reports',
    summary:
      "Spend per branch, biggest first, with each branch's share of the window.",
    description:
      'Spend per branch, biggest first, with each branch\'s share of the window.\n\nThe same rows as `GET /api/v1/reports/spend/by-site`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/by-site.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/over-time',
    tag: 'reports',
    summary: 'The trend chart.',
    description:
      'The trend chart. Empty buckets are filled with zeros rather than omitted: a chart that skips quiet days draws a straight line through them and overstates the week.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/over-time/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/over-time.csv',
    tag: 'reports',
    summary: 'Spend and orders per period across the window.',
    description:
      'Spend and orders per period across the window.\n\nThe same rows as `GET /api/v1/reports/spend/over-time`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/over-time.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/over-time.xlsx',
    tag: 'reports',
    summary: 'Spend and orders per period across the window.',
    description:
      'Spend and orders per period across the window.\n\nThe same rows as `GET /api/v1/reports/spend/over-time`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [{ permission: 'REPORT_VIEW', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/over-time.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/spend/summary',
    tag: 'reports',
    summary:
      'Spend, orders, average order value and growth against the previous window of the same length.',
    auth: 'bearer',
    permissions: [Permission.REPORT_VIEW],
    roles: [],
    body: [],
    query: [reportValidation.ReportRangeQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/reports/spend/summary/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/users/access-review',
    tag: 'reports',
    summary:
      'Every user with access to one account: role, primary and additional branches, per-user permission…',
    description:
      'Every user with access to one account: role, primary and additional branches, per-user permission overrides, and when they last signed in (SOW §15 "user access review extract — by account"; §12: "quarterly extract of every active user, role, site association and last login, for client attestation").\n\nActive users only unless `includeInactive=true`, which adds everyone else, deactivated users included. Expired permission grants are left out — they confer nothing, and a reviewer should not be asked to attest to them.\n\nUSER_MANAGE rather than REPORT_VIEW: it is the same information the user list already shows to whoever holds that permission, and no more.',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [],
    query: [reportValidation.AccessReviewQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      {
        status: 403,
        code: 'FORBIDDEN',
        description:
          'A non-administrator naming an account other than their own.',
      },
    ],
    conditionalPermissions: [{ permission: 'USER_MANAGE', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/users/access-review/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/users/access-review.csv',
    tag: 'reports',
    summary:
      'Every user with access, their role, branches, permission overrides and last sign-in, for…',
    description:
      'Every user with access, their role, branches, permission overrides and last sign-in, for attestation.\n\nThe same rows as `GET /api/v1/reports/users/access-review`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). CSV with a byte-order mark, so Excel reads names with macrons correctly, and with every cell through the shared formula guard.',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [],
    query: [reportValidation.AccessReviewQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces: 'text/csv',
    conditionalPermissions: [{ permission: 'USER_MANAGE', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/users/access-review.csv/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/reports/users/access-review.xlsx',
    tag: 'reports',
    summary:
      'Every user with access, their role, branches, permission overrides and last sign-in, for…',
    description:
      'Every user with access, their role, branches, permission overrides and last sign-in, for attestation.\n\nThe same rows as `GET /api/v1/reports/users/access-review`, with the columns declared once in `report-tables.ts` for both file formats (SOW §15: "CSV and XLSX for every tabular report"). Numbers as numbers — money, counts and percentages can be summed and filtered without retyping — and an About sheet saying who pulled it, when, and with which filters.',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [],
    query: [reportValidation.AccessReviewQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    produces:
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    conditionalPermissions: [{ permission: 'USER_MANAGE', when: 'Always.' }],
    rateLimited: false,
    source: 'src/app/api/v1/reports/users/access-review.xlsx/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/settings',
    tag: 'settings',
    summary: 'One form over two tables.',
    description:
      'One form over two tables. The settings row is created on first read rather than seeded when the account is, so accounts predating the table behave identically to new ones with no backfill to keep in step.',
    auth: 'bearer',
    permissions: [Permission.ACCOUNT_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/settings/route.ts',
  },
  {
    method: 'patch',
    path: '/api/v1/settings',
    tag: 'settings',
    summary:
      'Both tables are written in one transaction: the approval threshold and the ordering rules are read…',
    description:
      'Both tables are written in one transaction: the approval threshold and the ordering rules are read together on every checkout, and a form that saved one but not the other would leave orders judged against a rule nobody chose.\n\nThe audit entry names the fields that moved and what they moved to — a line reading only "settings updated" cannot answer who turned approvals off.',
    auth: 'bearer',
    permissions: [Permission.ACCOUNT_MANAGE],
    roles: [],
    body: [settingsValidation.UpdateSettingsSchema],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/settings/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/shipping/addresses',
    tag: 'shipping',
    summary: 'NZ Post address suggestions for the checkout type-ahead.',
    description:
      "Backed by ParcelAddress. At least four characters, because NZ Post refuses fewer; NZ Post suggests the client starts calling after five typed characters, which is the client's debounce to choose.\n\nReturns `addressId`s to pass to `PUT /cart/shipping/address`. The suggestion text is for display only — the address is fetched again when it is chosen.",
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [],
    query: [shippingValidation.AddressSearchQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    errors: [
      {
        status: 503,
        code: 'DEPENDENCY_UNAVAILABLE',
        description:
          'NZ Post could not be reached, shipping is switched off, or address search is not available to this portal yet.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/shipping/addresses/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/shipping/addresses/{addressId}',
    tag: 'shipping',
    summary: 'One validated NZ Post address, split into parts.',
    description:
      'Street number, street, suburb, city, postcode, DPID and whether it is a rural delivery. For showing the buyer what they picked; choosing it for the basket is `PUT /cart/shipping/address`.',
    auth: 'bearer',
    permissions: [Permission.ORDER_CREATE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/shipping/addresses/[addressId]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/shipping/pickups',
    tag: 'shipping',
    summary: 'Courier pickups booked, and parcels still waiting for one.',
    description:
      '`bookings` is a page of past and upcoming pickups. `awaitingPickup` is every labelled parcel not yet on a booking — what `POST` books when it is given no shipment ids.',
    auth: 'bearer',
    permissions: [Permission.ORDER_MANAGE],
    roles: [],
    body: [],
    query: [shippingValidation.ListPickupsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/shipping/pickups/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/shipping/pickups',
    tag: 'shipping',
    summary: 'Book an NZ Post courier pickup at the warehouse.',
    description:
      'Books the named labelled shipments, or every one waiting when none are named, at the configured ship-from address. Not retried automatically: a courier booked twice turns up twice. A booking NZ Post rejects is recorded as REJECTED with its reject code, and its parcels stay waiting.\n\nResending with the same `idempotencyKey` answers 200 with the earlier booking and does not call NZ Post.',
    auth: 'bearer',
    permissions: [Permission.ORDER_MANAGE],
    roles: [],
    body: [shippingValidation.BookPickupSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200, 201],
    errors: [
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description:
          'Nothing is waiting, or a named shipment is not labelled, is voided or is already booked (`details.notBookable`).',
      },
      {
        status: 503,
        code: 'DEPENDENCY_UNAVAILABLE',
        description:
          'Shipping is switched off, live pickups are not configured, or NZ Post could not be reached.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/shipping/pickups/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/shipping/shipments',
    tag: 'shipping',
    summary: 'The fulfilment queue of NZ Post labels.',
    description:
      'Across every account for an administrator, newest first, with the order number, branch and account alongside each. Filter to `status=FAILED` for the labels waiting on a retry, `awaitingPickup=true` for parcels not yet on a pickup, or `flagged=true` for labels the daily check found unscanned.',
    auth: 'bearer',
    permissions: [Permission.ORDER_MANAGE],
    roles: [],
    body: [],
    query: [shippingValidation.ListShipmentsQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/shipping/shipments/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/shipping/status',
    tag: 'shipping',
    summary: 'NZ Post integration health and configuration.',
    description:
      'The mode (disabled, mock or live), which capabilities could make live calls and which environment variables each still needs, and counts of labels by status — FAILED is the list waiting on a replay. With `probe=true` it also obtains an OAuth token, to prove the credentials work; the token itself is never returned.',
    auth: 'bearer',
    permissions: [Permission.INTEGRATION_MANAGE],
    roles: [],
    body: [],
    query: [shippingValidation.ShippingStatusQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/shipping/status/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/sites',
    tag: 'sites',
    summary:
      'Cursor-paginated: a branch list is scrolled, and offset pagination on a growing table skips or…',
    description:
      'Cursor-paginated: a branch list is scrolled, and offset pagination on a growing table skips or repeats rows when data shifts between pages.\n\nAPPLICATION_VIEW, not SITE_MANAGE — every signed-in user needs to know which branches exist to pick a delivery address. Editing them is the stricter permission below.',
    auth: 'bearer',
    permissions: [Permission.APPLICATION_VIEW],
    roles: [],
    body: [],
    query: [siteValidation.ListSitesQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/sites/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/sites',
    tag: 'sites',
    summary: 'A branch, with at least one address.',
    auth: 'bearer',
    permissions: [Permission.SITE_MANAGE],
    roles: [],
    body: [siteValidation.CreateSiteSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/sites/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/sites/{siteId}',
    tag: 'sites',
    auth: 'bearer',
    permissions: [Permission.APPLICATION_VIEW],
    roles: [],
    body: [],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/sites/[siteId]/route.ts',
  },
  {
    method: 'patch',
    path: '/api/v1/sites/{siteId}',
    tag: 'sites',
    summary:
      '`monthlyBudget` distinguishes omitted from explicitly null: the first leaves the cap alone, the…',
    description:
      '`monthlyBudget` distinguishes omitted from explicitly null: the first leaves the cap alone, the second removes it. Collapsing them would make an uncapped branch unreachable.',
    auth: 'bearer',
    permissions: [Permission.SITE_MANAGE],
    roles: [],
    body: [siteValidation.UpdateSiteSchema],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/sites/[siteId]/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/sites/{siteId}',
    tag: 'sites',
    summary: 'Soft: historical orders and invoices reference the branch.',
    description:
      'Soft: historical orders and invoices reference the branch. Deactivating is what stops new orders being placed against it.',
    auth: 'bearer',
    permissions: [Permission.SITE_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [204],
    rateLimited: false,
    source: 'src/app/api/v1/sites/[siteId]/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/sites/{siteId}/addresses',
    tag: 'sites',
    summary:
      'Exactly one default per branch and kind: setting a new default clears the old one first, so the…',
    description:
      "Exactly one default per branch and kind: setting a new default clears the old one first, so the pair can never both be default and leave the checkout picker's pre-selection non-deterministic.\n\nSend `nzPostAddressId` from `GET /api/v1/shipping/addresses` to validate it through NZ Post as it is saved: the address is stored with NZ Post's lines, DPID and rural flag (SOW F-16).",
    auth: 'bearer',
    permissions: [Permission.SITE_MANAGE],
    roles: [],
    body: [siteValidation.AddSiteAddressSchema],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [201],
    errors: [
      {
        status: 404,
        code: 'NOT_FOUND',
        description: 'NZ Post does not recognise `nzPostAddressId`.',
      },
      {
        status: 503,
        code: 'DEPENDENCY_UNAVAILABLE',
        description:
          '`nzPostAddressId` was sent while NZ Post is switched off or unreachable.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/sites/[siteId]/addresses/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/sites/{siteId}/addresses/{addressId}/nzpost',
    tag: 'sites',
    summary:
      'Validates a saved branch address through NZ Post ParcelAddress (SOW F-16), for an address entered…',
    description:
      "Validates a saved branch address through NZ Post ParcelAddress (SOW F-16), for an address entered before validation or without it. Send the id of the matching result from `GET /api/v1/shipping/addresses`; the address takes NZ Post's lines (a typed second line is kept), its DPID and its rural flag, and checkout defaults to it with those from then on.",
    auth: 'bearer',
    permissions: [Permission.SITE_MANAGE],
    roles: [],
    body: [siteValidation.ValidateSiteAddressSchema],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [200],
    errors: [
      {
        status: 404,
        code: 'NOT_FOUND',
        description:
          'No such saved address on this branch, or NZ Post does not recognise `nzPostAddressId`.',
      },
      {
        status: 503,
        code: 'DEPENDENCY_UNAVAILABLE',
        description: 'NZ Post is switched off or unreachable.',
      },
    ],
    rateLimited: false,
    source:
      'src/app/api/v1/sites/[siteId]/addresses/[addressId]/nzpost/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/templates',
    tag: 'templates',
    summary: 'The gallery.',
    description:
      'The gallery. TEMPLATE_USE, which every customer role holds — browsing the library is the point of it. An administrator additionally sees drafts; everyone else sees published templates that are unrestricted or granted to their account.',
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [],
    query: [templateValidation.ListTemplatesQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/templates/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/templates',
    tag: 'templates',
    summary: 'A new draft.',
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [templateValidation.CreateTemplateSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/templates/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/templates/{templateId}',
    tag: 'templates',
    summary:
      'What the builder opens: the draft, its design document, its assets and its version history.',
    description:
      'What the builder opens: the draft, its design document, its assets and its version history.\n\nThis is **not** what a buyer personalises — see `/customise`, which returns the published snapshot instead. A designer mid-rework must not change the artwork somebody is halfway through ordering.',
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/templates/[templateId]/route.ts',
  },
  {
    method: 'patch',
    path: '/api/v1/templates/{templateId}',
    tag: 'templates',
    summary: 'A save from the builder, autosave included.',
    description:
      'A save from the builder, autosave included. Send `expectedVersion` and a concurrent save by another designer comes back as 409 rather than silently overwriting them.',
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [templateValidation.UpdateTemplateSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/templates/[templateId]/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/templates/{templateId}',
    tag: 'templates',
    summary: 'Soft; published versions survive.',
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: false,
    source: 'src/app/api/v1/templates/[templateId]/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/templates/{templateId}/assets',
    tag: 'templates',
    summary: 'Attaches a library file.',
    description:
      "Names a file in the document library by `folderPath` and `fileName`; upload it with `POST /api/v1/dam/files` first. The portal copies the bytes into object storage and reads the filename, content type and size off the file itself, so there is no key, size or type for a caller to get wrong.\n\nTHUMBNAIL and PREVIEW replace the previous one rather than joining it — both are singular by nature. The library file behind the replaced one is left alone: it is the operator's own artwork, not a by-product.",
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [templateValidation.AttachTemplateAssetSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    errors: [
      {
        status: 404,
        code: 'NOT_FOUND',
        description: 'No file of that name in that library folder.',
      },
      {
        status: 422,
        code: 'BUSINESS_RULE_VIOLATION',
        description:
          'The library gave no link to the file, or it is empty or above the 50MB copy limit.',
      },
      {
        status: 503,
        code: 'DEPENDENCY_UNAVAILABLE',
        description:
          'The library is switched off, or DAM_SERVICE_LOGIN and DAM_SERVICE_PASSWORD are not configured.',
      },
    ],
    rateLimited: false,
    source: 'src/app/api/v1/templates/[templateId]/assets/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/templates/{templateId}/assets/{assetId}',
    tag: 'templates',
    summary: 'Detaches the asset.',
    description:
      "Detaches the asset. The stored object is deliberately left behind: a published version's design may still reference it, and storage is cheap next to a template that renders with a hole in it.",
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [204],
    rateLimited: false,
    source: 'src/app/api/v1/templates/[templateId]/assets/[assetId]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/templates/{templateId}/customise',
    tag: 'templates',
    summary:
      'What the buyer personalises: the **published snapshot**, not the draft.',
    description:
      'What the buyer personalises: the **published snapshot**, not the draft. A template whose draft has moved on since publication still hands the buyer the artwork that was published.\n\nA template that does not exist and one not published to this account answer identically, so the library cannot be enumerated by watching which ids differ.',
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/templates/[templateId]/customise/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/templates/{templateId}/customise',
    tag: 'templates',
    summary:
      'Checks a personalisation before it goes in the basket, and returns the accepted set together with…',
    description:
      'Checks a personalisation before it goes in the basket, and returns the accepted set together with the version it was checked against — which is what a cart line stores, so the basket records the artwork the buyer actually saw.\n\nA value aimed at a layer the designer locked is **refused**, not dropped: silently discarding it is a buyer who thinks they set their phone number and receives five hundred flyers without it. Unknown keys are refused for the same reason.',
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [templateValidation.CustomiseTemplateSchema],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/templates/[templateId]/customise/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/templates/{templateId}/publish',
    tag: 'templates',
    summary:
      'Freezes the current draft as a version and points the storefront at it.',
    description:
      'Freezes the current draft as a version and points the storefront at it. Republishing is the same operation — it cuts a new version and moves the pointer — which is why this is its own endpoint rather than a status change.',
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [templateValidation.PublishTemplateSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/templates/[templateId]/publish/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/templates/{templateId}/status',
    tag: 'templates',
    summary: 'DRAFT, PUBLISHED and ARCHIVED.',
    description:
      'DRAFT, PUBLISHED and ARCHIVED. Publishing is deliberately *not* reachable here — it has to cut a version, and a status change that silently did that would hide the one act with a lasting consequence behind the one without.',
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [templateValidation.ChangeTemplateStatusSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/templates/[templateId]/status/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/templates/{templateId}/versions',
    tag: 'templates',
    summary: 'Every snapshot, newest first.',
    description:
      'Every snapshot, newest first. Restoring one copies it back over the draft without deleting anything in between — the point of a history is that it does not lose the thing you restored from.',
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/templates/[templateId]/versions/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/templates/{templateId}/versions',
    tag: 'templates',
    summary:
      'Cuts a restore point from the current draft without publishing it.',
    description:
      'Cuts a restore point from the current draft without publishing it. Reuses the version number the draft is already on, so snapshotting twice at the same version is a no-op rather than an error.',
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [templateValidation.SnapshotTemplateSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/templates/[templateId]/versions/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/templates/{templateId}/versions/restore',
    tag: 'templates',
    summary: 'Copies an old version back over the draft.',
    description:
      'Copies an old version back over the draft. The restore is itself a save, so it bumps `version` and can be undone by restoring the version it replaced.',
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_USE],
    roles: [],
    body: [templateValidation.RestoreVersionSchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/templates/[templateId]/versions/restore/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/templates/{templateId}/visibility',
    tag: 'templates',
    summary:
      'The granted-account list is replaced wholesale rather than diffed: it is short, the operation is…',
    description:
      'The granted-account list is replaced wholesale rather than diffed: it is short, the operation is rare, and a diff is a place for a stale grant to survive.',
    auth: 'bearer',
    permissions: [Permission.TEMPLATE_MANAGE],
    roles: [],
    body: [templateValidation.SetTemplateVisibilitySchema],
    query: [],
    extraQuery: [],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/templates/[templateId]/visibility/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/users',
    tag: 'users',
    summary:
      'Creating users is not here: portal-native and external ones arrive through an invitation…',
    description:
      'Creating users is not here: portal-native and external ones arrive through an invitation, replicated ones through the legacy provisioning on first login.',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [],
    query: [userValidation.ListUsersQuerySchema],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/users/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/users/{userId}',
    tag: 'users',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/users/[userId]/route.ts',
  },
  {
    method: 'patch',
    path: '/api/v1/users/{userId}',
    tag: 'users',
    summary: 'Role, status and branch attachment.',
    description:
      'Role, status and branch attachment. Any of the three makes every live access token stale — the token carries all of them — so the refresh family is revoked and the change takes effect within the access TTL rather than in thirty days.\n\nYou cannot change your own role or disable yourself: not a security control, since a peer with USER_MANAGE can still do it, but the mistake is unrecoverable through the API that made it.',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [userValidation.UpdateUserSchema],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/users/[userId]/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/users/{userId}',
    tag: 'users',
    summary: 'Soft, and ends every session.',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [204],
    rateLimited: false,
    source: 'src/app/api/v1/users/[userId]/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/users/{userId}/permissions',
    tag: 'users',
    summary: 'The per-user departures from the role baseline.',
    description:
      'The per-user departures from the role baseline. The baseline itself is compiled in and read from `/authorization/permissions`.',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/users/[userId]/permissions/route.ts',
  },
  {
    method: 'post',
    path: '/api/v1/users/{userId}/permissions',
    tag: 'users',
    summary:
      'Grants or denies one permission, optionally scoped to a single resource.',
    description:
      'Grants or denies one permission, optionally scoped to a single resource. A repeat grant updates the existing row rather than adding a second: the unique index cannot enforce that itself, because PostgreSQL treats NULL resource ids as distinct.\n\nDENY always beats ALLOW when the two are resolved, whatever order the rows were written in.',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [userValidation.GrantPermissionSchema],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [201],
    rateLimited: false,
    source: 'src/app/api/v1/users/[userId]/permissions/route.ts',
  },
  {
    method: 'delete',
    path: '/api/v1/users/{userId}/permissions',
    tag: 'users',
    summary: 'Removes one grant by name.',
    auth: 'bearer',
    permissions: [Permission.USER_MANAGE],
    roles: [],
    body: [userValidation.RevokePermissionSchema],
    query: [],
    extraQuery: ['accountId'],
    successStatuses: [204],
    rateLimited: false,
    source: 'src/app/api/v1/users/[userId]/permissions/route.ts',
  },
  {
    method: 'get',
    path: '/api/v1/users/me/permissions',
    tag: 'users',
    summary: "The role baseline with the caller's own grants applied.",
    description:
      "The role baseline with the caller's own grants applied. The frontend uses it to decide which navigation and actions to render — it is a convenience, not the enforcement point, which is always the guard on the endpoint itself.\n\nNo permission is required: everyone may ask what they themselves can do.",
    auth: 'bearer',
    permissions: [],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/api/v1/users/me/permissions/route.ts',
  },
  {
    method: 'get',
    path: '/health',
    tag: 'health',
    summary: 'Service summary.',
    description:
      'For a human asking "what is actually deployed here": the release, the environment and how long this instance has been up. Not a probe; it checks nothing and always answers 200.',
    auth: 'public',
    permissions: [],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/health/route.ts',
  },
  {
    method: 'get',
    path: '/health/dependencies',
    tag: 'health',
    summary: 'Every dependency, including the non-critical ones.',
    description:
      'Includes the legacy database, whose outage degrades first-time logins but does not make the service unready. Answers 503 if any of them is down, so it can be pointed at by an uptime monitor without being wired to the load balancer.',
    auth: 'public',
    permissions: [],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/health/dependencies/route.ts',
  },
  {
    method: 'get',
    path: '/health/live',
    tag: 'health',
    summary: 'Liveness probe.',
    description:
      'Touches nothing. Failing it means the process is wedged and should be restarted; a database outage must never reach this handler, which is why the readiness probe is a separate path.\n\nUnauthenticated and unversioned, like every probe here: an orchestrator has no bearer token, a 401 reads to it as "unhealthy", and a health check should not need updating when the API moves from v1 to v2.',
    auth: 'public',
    permissions: [],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/health/live/route.ts',
  },
  {
    method: 'get',
    path: '/health/ready',
    tag: 'health',
    summary: 'Readiness probe.',
    description:
      '503 when anything it checks is down, which tells the load balancer to stop sending traffic. It deliberately does not say "restart me": the database may simply be failing over, and restarting every replica over a ten-second failover turns a blip into an outage.\n\nThe legacy database is absent on purpose — it is not required to serve traffic. See `/health/dependencies` for its status.',
    auth: 'public',
    permissions: [],
    roles: [],
    body: [],
    query: [],
    extraQuery: [],
    successStatuses: [200],
    rateLimited: false,
    source: 'src/app/health/ready/route.ts',
  },
]
